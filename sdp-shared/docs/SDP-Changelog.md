<img src="images/SDP_DocsLogo_WithText_0700x0163.png" alt="SDP Logo" width="375">

# SDP Bootstrap Changelog

Version history for `SDP_Sapient-Driven-Principles_v*.md`.
Add new entries at the top of the table — newest version first.

| Version | Patterns | Date | Summary |
|---------|----------|------|---------|
| 1.1.1 | 0 | 2026-08-13 | Version bump only — no new bootstrap-doc content. Every substantive change since v1.1.0 (2026-07-18) shipped as a "no version bump" entry below (solution-scoped phases 1-7 + dependency ledger, the phases-1-7 dispatch-gap fix, Phase 1/3 interactive brainstorm capture, Material Decision Escalation, the Responsive Web Design requirement, and automatic session-subagent-budget respawn) — the doc's own text already reflects all of it via its existing dated Addition/Correction blocks, so this bump introduces no new wording. User renamed `SDP_Sapient-Driven-Principles_v1.1.0.md` → `_v1.1.1.md` and updated `SDP-Document-List.json` directly; agent completed the sync per the Version Change Procedure (header `Version`/`Updated`/`File` fields and the closing footer stamp) once authorized. No `v1.1.0.md` historical copy was preserved — consistent with this repo's own prior precedent (`v1.0.0.md` was likewise removed rather than kept, per `d862c5b`).
| 1.1.0 | 0 | 2026-08-11 | No version bump (feature addition — automatic session respawn before Claude Code's hard subagent-spawn cap). Per `~SDP-Maintenance/~docs/done/subagent-budget-respawn-design.md` (dated 2026-07-28, revised 2026-08-11 after a 10-item Pros-Cons-Gaps pass): a real incident — a recurring `sdp-solution-state-loop` hit Claude Code's hard per-session, cumulative-lifetime subagent-spawn cap (`200 of 200 agents spawned`) after ~19 hours of unattended operation, since there is no native mechanism for a session to end itself or hand off gracefully. **Scope, per `sdp-core-invariants.md` #8 (Skill Scope Boundary):** only `sdp-solution-state-loop` is modified — this is a solution-level concern, and applying the identical fix to `sdp-project-state-loop` is explicitly deferred to a separate later change. The new condition is named **RESPAWN**, deliberately not "halt" (`workflow_status` stays `"active"` throughout — the workflow isn't blocked, one OS process is retired in favor of a fresh one). Implemented in `7e4deb0` (2026-08-11 18:07), confirmed end-to-end with a real forced-respawn test (terminal launch, cross-session self-registration, handoff confirmation, clean stop with no orphaned cron): new `SDP-Config.json` block `sessionSubagentBudget` (`respawnAtCount: 150` — a 150-agent soft tier that cancels the recurring cron and launches a replacement terminal while letting this fire's own already-due dispatch complete; `hardStopCount: 190` — a second tier, added during the Pros-Cons-Gaps pass to close a single-fire-overshoot gap, that additionally skips this fire's own dispatch entirely rather than risk a mid-dispatch crash; `autoRespawn` toggle; `maxSubagentsPerSession: 200` informational); three new Level-1-only scriptable skills — `sdp-claude-session-get-sessionid` (marker-based session discovery: embeds a plain-ASCII marker in its own tool call, polls the hook-log for it, since `session_id` is otherwise only available inside hook input, never to a running session directly), `sdp-claude-session-get-agentcount` (counts distinct `agent_id` values under a session — an exact match to the platform's own cap, not an approximation), and `sdp-claude-session-init` (the fresh terminal's wrapped initial prompt: self-registers via the launcher's session row, confirms the handoff, then chains into `/sdp-state-loop-start`); `sdp-claude-new-terminal` extended with a `-callerSessionId` parameter, written onto the new instance's registry row as `launcherSessionId` in the same write that creates it; `sdp-preflight.ps1` gained a config-sanity check for the new threshold ordering. **Bundled bug fix, same commit:** `sdp-cancel-auto` matched only `sdp-project-state-loop` cron jobs, never `sdp-solution-state-loop` — unlike its three sibling skills, which already checked both — meaning `/sdp-cancel-auto` would have silently failed to cancel a running solution-level loop; fixed to match either. Confirmed present in the current repo: `SDP-Config.json`'s `sessionSubagentBudget` block (both tiers) and all three new skills' L1 shims. **Design doc note:** the commit message records the design doc as deleted per user direction at implementation time — consistent with this whole docs-review pass, where the user restored it (along with several others) into `~SDP-Maintenance/~docs/done/` for retroactive changelog reconciliation. **Entry added retroactively** during a 2026-08-13 docs-review pass — verified against live repo content and git history at review time, not from a contemporaneous implementation-session transcript.
| 1.1.0 | 0 | 2026-08-11 | No version bump (GPG doc + skill/template changes, no bootstrap-doc edit). Added a Multi-Platform (Responsive) Web Design requirement, closing a gap where a full-text search of the GPG standards doc for responsive/breakpoint/viewport/etc. returned zero matches and Chapter 12 ("Mobile Readiness") turned out to be a false friend (native-MAUI API prep, not website responsiveness) — see `~SDP-Maintenance/~docs/done/responsive-design-requirement-design.md` for the full design record (three options adopted: A+C+D). Changes verified present in the repo: (1) a new "Responsive Layout" subsection (MUST/SHOULD/MAY rules — viewport meta tag, Flexbox/Grid with mobile+tablet breakpoints, collapsing navigation, three-reference-width verification) added to Chapter 11 in both `standards/GenericProjectGuidlines_V1.10_20260323.md` and the mirrored `standards/GenericProjectGuidlines_Sections/GenericProjectGuidlines_WebsiteBlazor.md`; (2) `sdp-project-gate-review/SKILL.md` and `sdp-solution-phase-gate-review/SKILL.md` Step 6 GPG-alignment criterion changed from topic-conditional ("chapter whose topic appears in the phase document") to coverage-forcing (a chapter applicable per the GPG Reading Map is a required finding-or-documented-rationale even when the phase document never mentions it — confirmed via direct Read of both files' Step 6 wording; the solution-scoped file correctly omits the project-level `gpg_excluded_chapters` exclusion clause, since that field doesn't exist yet at solution scope); (3) `sdp-shared/docs/SDP-Workspace-Setup.md`'s `.speq` Contract Template gained a "Standing Non-Functional Requirements" table for cross-cutting requirements that bind every task of a matching kind; (4) `sdp-shared/ai-skills/sdp-solution-phase-coordinator/SKILL.md` Step 2b item 0 now transcribes any standing non-functional requirement from `04_architecture.md`/`05_implementation_overview.md` into that new `.speq` section at Phase 7; (5) `sdp-project-worker/SKILL.md` and `sdp-project-reviewer/SKILL.md` (Step 3.1/3.3 and Step 3.1/3.2 respectively) now treat a matching Standing Non-Functional Requirements row as explicit, binding acceptance-criteria input — REVIEWER forms this independently before reading WORKER's Completed blockquote, preserving Role Separation. The design doc's own bootstrap-doc-untouched decision (Option E not pursued) and two residual gaps (no project-type-as-data mechanism in SDP; no retroactive `.speq` backfill for solutions that passed Phase 7 before this shipped) are recorded as-is, not re-litigated here. **Entry added retroactively** during a 2026-08-13 docs-review pass — verified against live repo content at review time, not from a contemporaneous implementation-session transcript.
| 1.1.0 | 0 | 2026-08-08 | No version bump (skill rename, no behavior change). `sdp-new-concept-intake` renamed to `sdp-solution-new-concept-intake` — second of two related renames, immediately following `sdp-source-coverage-check` → `sdp-solution-source-coverage-check` below — corrects the same naming inconsistency: this skill is solution-scoped in every way it behaves (drop zone `sdp-solution-docs/user-design-docs/`, writes to `.sdp-solution-workflow/registry.md` and `sdp-solution-docs/01_concept.md`) but never received the `sdp-solution-` prefix carried by every other solution-scoped skill (see `~SDP-Maintenance/~docs/project-scope-skill-naming-design.md` §2 and §8.3/§9 for the identical-shape precedent this rename follows). Renamed via `git mv` to preserve history: `.claude/skills/`, `sdp-shared/ai-skills/`. `SDP-Solution-Setup.json` (`skill-pair` entry) updated. Self-references: `.claude/skills/sdp-solution-new-concept-intake/SKILL.md` (frontmatter `name`, both `-skillName` tone-invocation calls, the Read-path and halt-message path) updated; the L2 file (`sdp-shared/ai-skills/sdp-solution-new-concept-intake/SKILL.md`) needed no self-reference edit — confirmed zero literal occurrences of its own name anywhere in its body, a known pre-existing quirk of this particular skill file, not a miss. Cross-references updated: `.claude/skills/sdp-phase-rightsizing-check/SKILL.md` (1, description), `.claude/skills/sdp-solution-source-coverage-check/SKILL.md` (1, description), `.claude/skills/sdp-workspace-setup/SKILL.md` (1, description), `sdp-shared/ai-skills/sdp-initialize-sdp/SKILL.md` (2), `sdp-shared/ai-skills/sdp-solution-phase-coordinator/SKILL.md` (1), `sdp-shared/ai-skills/sdp-solution-phase-worker/SKILL.md` (1), `sdp-shared/ai-skills/sdp-solution-source-coverage-check/SKILL.md` (2), `sdp-shared/ai-skills/sdp-solution-state-loop/SKILL.md` (2), `sdp-shared/ai-skills/sdp-workspace-setup/SKILL.md` (5) — the last two skills' own self-identity (already `sdp-solution-source-coverage-check`/correctly scoped) left fully untouched, only their mentions of *this* skill updated. Narrative docs: `sdp-shared/docs/SDP-Workspace-Setup.md` (3), `sdp-shared/docs/SDP-Flowchart.md` (5), `README.md` (5), `QuickStart.md` (5), `sdp-solution-docs/user-design-docs/README.md` (4), `sdp-solution-docs/user-design-docs/processed/README.md` (1). Bootstrap doc (`SDP_Sapient-Driven-Principles_v1.1.0.md`)'s 5 literal mentions: 1 (the Phase 1/3 Interactive Capture Mechanics step 1a note, `source_document` field reference) was not already inside a dated blockquote's own narrative, so it got a fresh strikethrough plus a new dated Correction appended immediately after (placed directly before the sibling rename's own 2026-08-08 correction already sitting in the same paragraph); the remaining 4 (the "Phase 1 — Source-Doc Intake Ownership" section's 2026-07-18 Addition blockquote, two occurrences, and its nested 2026-07-20 Correction blockquote, two occurrences) fell inside those existing dated blockquotes' own narrative text and were struck through in place with no new correction needed, per Append-Only Discipline. Also updated, as living tracking indexes rather than point-in-time snapshots (same treatment given to these two files in every prior rename entry): `~SDP-Maintenance/~docs/skill-catalogue.md` (3 mentions incl. the Hybrid design-session-first summary list) and `~SDP-Maintenance/~docs/sdp-create-banner-adoption-tracker.md` (1 per-skill progress row). Left unchanged, deliberately: `~SDP-Maintenance/~sdp-shared/~skill-evals/sdp-new-concept-intake-eval.md` (dated skill-evaluation snapshot), `~SDP-Maintenance/~sdp-maintenance-scripts/reports/skill-script-audit-report-20260722-1400.md` / `-20260724-2135.md` / `-20260724-2142.md` / `-20260724-2143.md` / `-20260724-2144.md` (dated audit snapshots), `~SDP-Maintenance/~official-change-logs/~ai-skills/~sdp-workspace-setup.md` (a frozen point-in-time changelog snapshot for a different skill, historical mention only), `.sdp-solution-workflow/logging/**` (raw historical event logs), and `project-scope-skill-naming-design.md` itself (the planning record of this very rename). Also judged out of scope, individually re-read and confirmed closed/dated design-decision records rather than live instructions (same class as prior renames' excluded design/plan docs): `~SDP-Maintenance/~docs/phase-pipeline-expansion-design.md`, `phase-pipeline-expansion-plan.md`, `phase-pipeline-expansion-exploration.md`, `solution-coordinator-orchestration-design.md`, `solution-coordinator-orchestration-plan.md`. Two design docs never checked against this skill by any prior rename, checked fresh: `~SDP-Maintenance/~docs/solution-phase-dispatch-gap-design.md` (self-declared "Status: Implemented" as of 2026-07-21 — its two mentions of this skill are historical narrative about which skills did/didn't need changes for a different, already-shipped feature) and `~SDP-Maintenance/~docs/subagent-budget-respawn-design.md` (dated 2026-07-28, "Status: Approved design — ready for implementation planning" for an unrelated cron/subagent-budget feature — its one mention of this skill is a factual note, as of that design's writing, about which sibling skills a cron-matching check covers; not a live instruction about this skill itself). No hit found, confirmed no-op: `.ps1` scripts (literal or constructed), `sdp-shared/scripts/tests/*.Tests.ps1`, `.claude/settings.local.json`, and `sdp-shared/scripts/script-support/SDP-Tones.json` — no exact-match reference to this skill in any of the four. |
| 1.1.0 | 0 | 2026-08-08 | No version bump (skill rename, no behavior change). `sdp-source-coverage-check` renamed to `sdp-solution-source-coverage-check` — corrects a naming inconsistency: this skill is solution-scoped in every way it behaves (drop zone `sdp-solution-docs/user-design-docs/`, writes to `.sdp-solution-workflow/registry.md` and `sdp-solution-docs/01_concept.md`/`03_expanded_concept.md`) but never received the `sdp-solution-` prefix carried by every other solution-scoped skill (see `~SDP-Maintenance/~docs/project-scope-skill-naming-design.md` §2 and §8.3/§9 for the identical-shape precedent this rename follows, applied to the 11 `sdp-project-*` renames). Renamed via `git mv` to preserve history: `.claude/skills/`, `sdp-shared/ai-skills/`. Every literal-identifier reference updated to match: `SDP-Solution-Setup.json` (`skill-pair` entry), the two SKILL.md files' own self-references (L1 frontmatter `name`, both tone-invocation `-skillName` calls, the L1 self-referencing Read-path and halt-message path, the L2 `sdp_source_coverage` `purpose` string), cross-references in `sdp-shared/ai-skills/sdp-solution-phase-coordinator/SKILL.md` (3 mentions), `sdp-shared/ai-skills/sdp-solution-phase-gate-review/SKILL.md` (1), `sdp-shared/ai-skills/sdp-solution-phase-worker/SKILL.md` (1), `sdp-shared/ai-skills/sdp-project-loop-prep/SKILL.md` (3) and `.claude/skills/sdp-project-loop-prep/SKILL.md` (description), `sdp-shared/ai-skills/sdp-solution-loop-prep/SKILL.md` (1) and `.claude/skills/sdp-solution-loop-prep/SKILL.md` (description), `sdp-shared/ai-skills/sdp-phase-rightsizing-check/SKILL.md` (2), and the `sdp-source-coverage-check`-only mentions inside `sdp-shared/ai-skills/sdp-new-concept-intake/SKILL.md` (8) and `.claude/skills/sdp-new-concept-intake/SKILL.md` (1) — that skill's own identity left untouched, its rename to `sdp-solution-new-concept-intake` is a separate follow-up pass. Narrative docs: `sdp-shared/docs/SDP-Workspace-Setup.md` (1), `sdp-shared/docs/SDP-Flowchart.md` (3), `README.md` (2), `QuickStart.md` (2), `sdp-solution-docs/user-design-docs/README.md` (1), `sdp-solution-docs/user-design-docs/processed/README.md` (1). Bootstrap doc (`SDP_Sapient-Driven-Principles_v1.1.0.md`)'s 5 literal mentions: 3 fell inside the existing 2026-07-18 "Phase 1 — Source-Doc Intake Ownership" Addition blockquote and were struck through in place per that block's own date, no new correction needed; the remaining 2 (the Phase 1/3 Interactive Capture Mechanics step 1a note, and the Phase 3 gate's Source coverage criterion) each got its own new dated Correction appended immediately after, per Append-Only Discipline. Also updated, as living tracking indexes rather than point-in-time snapshots (same treatment given to these two files in every prior rename entry): `~SDP-Maintenance/~docs/skill-catalogue.md` (3 mentions incl. the LLM-Only list) and `~SDP-Maintenance/~docs/sdp-create-banner-adoption-tracker.md` (1 per-skill progress row). Left unchanged, deliberately: `~SDP-Maintenance/~sdp-shared/~skill-evals/sdp-source-coverage-check-eval.md` and `sdp-loop-prep-eval.md` (dated skill-evaluation snapshots — the latter's one mention confirmed genuine, not a grep false-positive, but still a point-in-time record), `~SDP-Maintenance/~sdp-maintenance-scripts/reports/skill-script-audit-report-20260722-1400.md` / `-20260724-2135.md` / `-20260724-2142.md` / `-20260724-2143.md` / `-20260724-2144.md` (dated audit snapshots), and `project-scope-skill-naming-design.md` itself (the planning record of this very rename). Also judged out of scope, individually re-read and confirmed closed/dated design-decision records rather than live instructions (same class as prior renames' excluded design/plan docs): `~SDP-Maintenance/~docs/phase-pipeline-expansion-design.md`, `phase-pipeline-expansion-plan.md`, `solution-coordinator-orchestration-design.md`, `solution-coordinator-orchestration-plan.md`. No hit found, confirmed no-op: `.ps1` scripts (literal or constructed), `sdp-shared/scripts/tests/*.Tests.ps1`, `.claude/settings.local.json`, and `sdp-shared/scripts/script-support/SDP-Tones.json` — no exact-match reference to this skill in any of the four. |
| 1.1.0 | 0 | 2026-07-28 | No version bump (skill rename, no behavior change). `sdp-new-claude-terminal` renamed to `sdp-claude-new-terminal`. Renamed via `git mv` to preserve history: `.claude/skills/` (folder + frontmatter `name` field), `sdp-shared/scripts/sdp-new-claude-terminal.ps1` → `sdp-claude-new-terminal.ps1` (no self-referencing content inside the script itself). The skill's own SKILL.md self-references updated: both `-skillName` tone-invocation calls and the script-invocation code block. `SDP-Solution-Setup.json`: 2 `file-exists` entries plus 1 `permissions.allow` entry updated in place. `SDP-Config.json`: `newTerminalNotes`' one `.ps1` reference updated. Narrative docs updated directly (pure rename, no substantive claim changed): `README.md` (3), `sdp-shared/docs/SDP-Workspace-Setup.md` (5), `sdp-shared/docs/SDP-Skill-Authoring.md` (1). Living tracking indexes updated directly, same treatment as prior rename precedent: `~SDP-Maintenance/~docs/skill-catalogue.md` (3), `~SDP-Maintenance/~docs/sdp-create-banner-adoption-tracker.md` (2). Bootstrap doc (`SDP_Sapient-Driven-Principles_v1.1.0.md`)'s 1 mention (inside the existing 2026-07-18 script-support relocation correction) struck through with a new dated Correction appended, per Append-Only Discipline. Confirmed excluded, per the same historical-record category established by the prior `sdp-coordinator` rename entry below: `~SDP-Maintenance/~sdp-shared/~skill-evals/sdp-new-claude-terminal-eval.md` (eval report — historical assessment, not living reference; filename left as-is), `~SDP-Maintenance/~docs/terminal-orchestration-exploration.md` (Status: "Exploration"), `~SDP-Maintenance/~sdp-maintenance-scripts/reports/skill-script-audit-report-*.md` (4 files — dated audit snapshots), `sdp-solution-docs/log-reports/combined-metrics/log-report-combined-metrics_*.md` (2 files — generated report snapshots), `.sdp-solution-workflow/logging/**` (raw historical event logs). Checked and confirmed no reference in any: `sdp-shared/scripts/script-support/SDP-Tones.json`, `sdp-shared/scripts/script-support/sdp-report-log-loop-metrics-skills.json` (not a role skill tracked there), `sdp-shared/scripts/tests/*.Tests.ps1`. |
| 1.1.0 | 0 | 2026-07-24 | No version bump (feature addition — Material Decision Escalation). Per `~SDP-Maintenance/~docs/done/material-decision-escalation-design.md`: a consuming project's Architecture and Implementation Overview phases ran fully autonomously, as designed, and picked Microsoft Azure as the hosting platform with zero user contact — the WORKER's own Gap Resolution entry stated outright "no GPG precedent to cite" and used the spec-compliant "declared out of scope with a written rationale" closure path to avoid ever asking, which the bootstrap doc's existing Gap Resolution Criteria always permitted. A second, related incident: a build stopped mid-stream needing an external API's credentials, discovered only when a downstream WORKER hit the wall directly. Implemented in `5ae9f39` (2026-07-24 14:03), bundled with unrelated in-progress work at the user's explicit direction: a new `SDP-Config.json` field `materialDecisionEscalation.enabled` (loop-owned, default `true` — same "no skill may write this field" discipline as `eval_cycle_attempts`) requiring any dispatched session, any role, project- or solution-scoped, to halt per the existing Halt Behavior Contract rather than silently settle an unnamed external dependency (language/runtime/framework/library/database/cloud-hosting-provider/third-party-API) or an architectural pattern with no GPG precedent — exempting anything already explicitly declared in `.speq`. **Deviated from the design doc's own stated scope** ("Level 2 skill-file mirroring... not edited in this pass" was listed as explicitly out of scope, reasoning that WORKER/REVIEWER already read the full bootstrap doc every session): the shipped commit added a "Material Decision Escalation Check" section directly to `sdp-project-worker`, `sdp-project-reviewer`, `sdp-project-coordinator`, `sdp-project-gate-review`, `sdp-solution-worker`, `sdp-solution-reviewer`, `sdp-solution-coordinator`, and the three `sdp-solution-phase-*` skills — confirmed via the commit's own message, not verified line-by-line against every file in this review pass. Bootstrap doc changes: new "Material Decision Escalation" section (Dispatch and Halt Contracts area); new Phase 4 (Architecture) Mechanics/Gate subsection — Phase 4 previously had no dedicated write-up at all, unlike Phase 2/3/7 — including the **External Dependency Completeness** gate criterion (GATE_REVIEWER confirms every Architecture-surfaced dependency is either approved in `.speq` or explicitly deferred with recorded user approval and a named target phase/task; GATE_BLOCKED otherwise), directly closing the mid-build-credentials failure mode; Gap Resolution Exemption sections updated for pre-Phase-7 settlement sources. REVIEWER's existing `.speq`-alignment check gained one clause: a deviation introducing a new external dependency is a finding unless a resolved Material Decision Escalation halt is on record for it. Same commit also bundled GPG guidance distinguishing executable code from data/config files, and a tone-config change (dedicated "tada" tune for `sdp-initialize-sdp`'s close). Confirmed present in the current repo: `SDP-Config.json`'s `materialDecisionEscalation` block, and the bootstrap doc's Phase 4 section (verified directly in an earlier pass of this same review). A later follow-up, `116d751` (2026-07-31), synced `SDP-Flowchart.md` to reflect Material Decision Escalation and Phase 7 `.speq` population. **Entry added retroactively** during a 2026-08-13 docs-review pass — verified against live repo content and git history at review time, not from a contemporaneous implementation-session transcript.
| 1.1.0 | 0 | 2026-07-24 | No version bump (bug fix, `sdp-project-coordinator`/`sdp-solution-phase-coordinator` SKILL.md, plus the bootstrap doc's Phase Readiness Regression Procedure). Surfaced by a consuming project: after a Phase Readiness regression's target phase completed a genuinely fresh WORKER → REVIEWER cycle and reached all-VERIFIED, `sdp-solution-phase-coordinator` re-halted on the *original* Remediation Proposals instead of dispatching a fresh gate — even though the user had already selected and fully executed that proposal. Root cause: the `"blocked"`-branch algorithm in both coordinators' Step 4 sub-step 5 / Step 2e only ever modeled one meaning of `phase_gate.status == "blocked"` — a real GATE_REVIEWER verdict already sitting in the phase document — with no branch for the *administrative* meaning the regression procedure itself creates (force-setting `"blocked"` on the target phase purely to require a fresh gate, with no blockquote written at all). The solution-scoped mirror compounded this by checking the fixed `sdp-solution-docs/07_phase_readiness.md` path unconditionally instead of the current phase's own document — under Append-Only Discipline that file's Remediation Proposals heading can never be removed, so every later `"blocked"` occurrence for *any* phase, for the rest of that solution's life, would misread as the same unresolved decision. A user-proposed alternative explanation (that the new Material Decision Escalation rule already closes this gap, since it prevents the kind of unapproved architecture decision that triggered this project's original block) was independently evaluated and rejected as a full fix: Material Decision Escalation only prevents its own two trigger types (undeclared external dependency, GPG-silent pattern) — a Phase Readiness gate can still block on rightsizing, dependency-ordering, acceptance-criteria, or plain traceability gaps unrelated to any decision needing approval, and the affected project's `07_phase_readiness.md` was already permanently poisoned by the one regression that had already occurred, independent of any future trigger. Fixed by adding an explicit disambiguation to both coordinators' `"blocked"` branches, keyed on `phase_gate.gate_review_attempts` (a real GATE_BLOCKED verdict always leaves it `>= 1`; only the regression procedure ever resets it to `0` against a `"blocked"` status, so `== 0` unambiguously signals the administrative case) — that branch now dispatches GATE_REVIEWER exactly like a first gate, with a `Re-Gate Trigger:` field noting the regression. The solution-level mirror now also resolves the current phase's own document (`sdp-solution-docs/[NN_phase_name].md`) instead of the hardcoded path. Bootstrap doc's Phase Readiness Regression Procedure section gained a dated Addition (2026-07-24) documenting the gap and the fix, per Append-Only Discipline — no prior text struck through, since the existing procedure description was accurate as stated, just missing this mechanical detail. Not yet verified against a live regression cycle in this repo (no active solution here) — logic-traced against the documented algorithm and the reporting project's transcript only. |
| 1.1.0 | 0 | 2026-07-24 | No version bump (bug fix, `sdp-auto`/`sdp-state-loop-start`/`sdp-project-loop-prep` SKILL.md, plus two narrative-doc echoes of the same misconception). Both skills' "Step 0: Await Session-Start Completion" section checked for the wrong skill: it looked for `sdp-project-read-docs` to determine whether session-start had finished, and used that skill's own Step 3 one-sentence confirmation as the completion signal. Confirmed factually wrong two ways: (1) `.claude/settings.local.json`'s live `hooks.SessionStart` config invokes `sdp-initialize-sdp` directly (`additionalContext = 'Before responding to any user message this session, invoke the sdp-initialize-sdp skill by running /sdp-initialize-sdp.'`) — `sdp-project-read-docs` is never named there at all; (2) `sdp-project-read-docs` is a project-scoped doc loader invoked separately, later, by other skills (e.g. `sdp-project-run-prompt`), with no necessary connection to session-start timing. Read `sdp-shared/ai-skills/sdp-initialize-sdp/SKILL.md` and `.claude/skills/sdp-initialize-sdp/SKILL.md` in full to determine its actual completion signal per its own Output Discipline/Outputs sections: opening banner, then an internal `sdp-solution-read-docs` invocation (never printed to chat), then the closing banner, then exactly one short closing statement — that closing banner plus closing statement is the real, only completion signal this skill emits. Fixed both files' Step 0 sub-steps 1-3 to check for `sdp-initialize-sdp` and this closing-banner-plus-closing-statement signal instead, updated both skills' pending-halt banner text (`awaiting session-start (sdp-initialize-sdp) completion`, was `(sdp-project-read-docs)`), and updated the matching "Do not begin Step 1 until..." Constraints bullet in both files to reference `sdp-initialize-sdp`'s closing banner/statement instead of `sdp-project-read-docs`'s Step 3 confirmation. No other content in either file touched. Checked the two Level 1 shim files (`.claude/skills/sdp-auto/SKILL.md`, `.claude/skills/sdp-state-loop-start/SKILL.md`) for the same claim: both are generic Read-and-execute-the-L2-file boilerplate with no session-start-completion logic of their own — left unmodified. Grepped the whole repo for `session-start hook triggered` to confirm full coverage: found the identical bug pattern in a third file, `sdp-shared/ai-skills/sdp-project-loop-prep/SKILL.md` (its own Step 0, checking for `sdp-project-read-docs` **or** `sdp-solution-read-docs`) — fixed directly by the orchestrating session using the identical correction pattern, since it's the same defect, not a new scope decision. A follow-up review pass then found the same underlying misconception echoed in two more places, both corrected: `.claude/skills/sdp-solution-read-docs/SKILL.md`'s own frontmatter `description` (previously stated it is itself "invoked at session start via the SessionStart hook" — corrected to attribute that to `sdp-initialize-sdp`, its actual direct target), and `~SDP-Maintenance/~docs/sdp-create-banner-adoption-tracker.md`'s Tier-3 rationale row for `sdp-solution-read-docs` (same correction, applied to its own living tracking-index content, not a frozen historical snapshot). Checked `SDP_Sapient-Driven-Principles_v1.1.0.md` directly (not just by trusting an earlier grep claim): its several `sdp-project-read-docs` mentions all correctly describe it as *not* auto-loaded at session start (the accurate, opposite claim) — no instance of this bug present, no Append-Only correction needed. `sdp-shared/scripts/sdp-preflight.ps1 -workspaceRoot .` re-run after the fix: same 2 pre-existing, unrelated failures only (`.sdp-solution-workflow/dependencies.md`/`.json` missing), no new failures. `sdp-shared/scripts/tests/sdp-preflight.Tests.ps1` via Pester: 35/35 passing, matching the existing baseline, zero regressions. |
| 1.1.0 | 0 | 2026-07-24 | No version bump (skill rename, no behavior change). `sdp-coordinator` renamed to `sdp-project-coordinator` — **eleventh and final** of the 11 project-scoped skills renamed under the `sdp-project-*` prefix convention (see `~SDP-Maintenance/~docs/project-scope-skill-naming-design.md` §8 for the full plan, §9 for the execution log, and the prior ten 2026-07-23/24 entries below documenting skills 1-10 of 11). This is the single most-referenced skill in the entire rename series — the central per-project dispatch skill every other skill's SKILL.md talks about. Renamed via `git mv` to preserve history: `.claude/skills/`, `sdp-shared/ai-skills/`. `SDP-Solution-Setup.json`: entry existed (`{ "type": "skill-pair", "name": "sdp-coordinator", "tier": "integrity" }`), `name` field updated in place — no missing-entry surprise. The two renamed SKILL.md files' own self-references updated: L1 frontmatter `name`, both `-skillName` tone-invocation calls, the Read-path and halt-message path referencing the L2 file; the L2 file's own 2 self-references (the "Scope note" line and the Constraints "Never use `sdp-coordinator`..." bullet). **Loop-metrics alias config, per the skill-7 bug-fix mechanism:** `sdp-shared/scripts/script-support/sdp-report-log-loop-metrics-skills.json` — the `COORDINATOR` role's `aliases` array (`["sdp-coordinator"]`) had `"sdp-project-coordinator"` appended per the file's own maintenance rule (oldest-first, current-name-last; never remove/reorder) — now `["sdp-coordinator", "sdp-project-coordinator"]`; `sdp-shared/scripts/sdp-report-log-loop-metrics.ps1` itself needed no logic change, confirmed: it already derives `$canonicalCoordinator` from this config at load time, with only 3 comment-only prose mentions of the literal skill name updated directly (no hardcoded bucketing array duplicating the JSON). Cross-references updated in every SKILL.md with a literal `sdp-coordinator` hit, carefully distinguishing from two collision-risk siblings throughout (`sdp-solution-coordinator` and `sdp-solution-phase-coordinator` — both confirmed, via a dedicated test-string regex check before trusting the whole-repo grep, to have no substring collision under word-boundary matching since `solution-`/`solution-phase-` sits between the `sdp-` prefix and `coordinator` in both names): `.claude/skills/sdp-solution-coordinator/SKILL.md` (2), `sdp-shared/ai-skills/sdp-solution-coordinator/SKILL.md` (20), `sdp-shared/ai-skills/sdp-solution-phase-coordinator/SKILL.md` (18), `sdp-shared/ai-skills/sdp-project-state-loop/SKILL.md` (20), `sdp-shared/ai-skills/sdp-auto/SKILL.md` (7), `sdp-shared/ai-skills/sdp-solution-worker/SKILL.md` (4), `sdp-shared/ai-skills/sdp-state-loop-start/SKILL.md` (3), `sdp-shared/ai-skills/sdp-solution-reviewer/SKILL.md` (2), `sdp-shared/ai-skills/sdp-solution-state-loop/SKILL.md` (2), `sdp-shared/ai-skills/sdp-project-run-prompt/SKILL.md` (2), `sdp-shared/ai-skills/sdp-project-loop-prep/SKILL.md` (2), `sdp-shared/ai-skills/sdp-new-concept-intake/SKILL.md` (2), `sdp-shared/ai-skills/sdp-workspace-setup/SKILL.md` (1), `sdp-shared/ai-skills/sdp-standards-setup/SKILL.md` (1), `sdp-shared/ai-skills/sdp-report-log-loop-metrics/SKILL.md` (1), `sdp-shared/ai-skills/sdp-project-gate-review/SKILL.md` (1), `sdp-shared/ai-skills/sdp-phase-rightsizing-check/SKILL.md` (1). `sdp-shared/scripts/sdp-create-prompt.ps1` (5 comment-only mentions) also updated — no logic constructs the skill name via concatenation. Narrative docs: `README.md` (8), `sdp-shared/docs/SDP-Workspace-Setup.md` (5), `sdp-shared/docs/SDP-Flowchart.md` (4), `sdp-shared/docs/SDP-Script-Authoring.md` (3), `sdp-shared/docs/SDP-Tone-Notifications.md` (3 — confirmed clean of any unrelated concurrent-work diff at edit time, unlike skill 10's handling of this same file; updated directly), `sdp-shared/docs/SDP-Project-Evolution.md` (1 — silent identifier update per the skill-9 precedent: the doc's self-declared "append-only" status protects its decisions/conclusions from silent reversal, not identifier-consistency maintenance across an unrelated rename). Also updated, as living tracking indexes rather than point-in-time snapshots (same treatment given to these two files in all ten prior entries): `~SDP-Maintenance/~docs/skill-catalogue.md` (2 mentions — catalogue row, LLM-Only summary-list entry) and `~SDP-Maintenance/~docs/sdp-create-banner-adoption-tracker.md` (1 — per-skill progress row). Bootstrap doc (`SDP_Sapient-Driven-Principles_v1.1.0.md`)'s 17 literal skill-name mentions corrected via strikethrough + 12 new dated Correction blockquotes appended immediately after each site, per Append-Only Discipline — not rewritten in place: several corrections consolidate 2-3 mentions clustered in the same paragraph or step (the Phase-Readiness solution-scope section around the 2026-07-21 correction; the three near-identical "0b. SKILLS CHECK" superseded-step skill lists in the WORKER-dispatch, REVIEWER-dispatch, and GATE_REVIEWER-dispatch procedure blocks; the Solution Dispatch Loop section), and several land inside pre-existing older Correction/Addition chains (2026-07-10 ×2, 2026-07-07, 2026-07-17 ×2) shared with skills 1-10, following the same nested-correction-inside-older-correction precedent as skills 6-10. Confirmed excluded, checked individually rather than by generic appeal (all closed, dated, point-in-time design/plan/exploration/changelog records whose mentions are historical narrative, not living reference documentation): `~SDP-Maintenance/~docs/phase-1-3-interactive-capture-design.md` (Status: "Implemented"), `~SDP-Maintenance/~docs/terminal-orchestration-exploration.md` (Status: "Exploration — not a design doc, not approved for implementation"), `~SDP-Maintenance/~docs/sdp-solution-setup-json-design.md` (Status: "Design" — answers a since-resolved proposal), `~SDP-Maintenance/~docs/solution-phase-dispatch-gap-design.md` (Status: "Implemented"), `~SDP-Maintenance/~docs/phase-pipeline-expansion-design.md` and its companion `~SDP-Maintenance/~docs/phase-pipeline-expansion-plan.md` (design/plan pair with embedded historical `git commit -m` templates, already executed), `~SDP-Maintenance/~docs/solution-coordinator-orchestration-design.md` and `~SDP-Maintenance/~docs/solution-coordinator-orchestration-plan.md` (design/plan pair, both already executed — the design doc is also explicitly cited as precedent by this rename series' own controlling design doc), `~SDP-Maintenance/~official-change-logs/~ai-skills/~sdp-workspace-setup.md` (a per-skill dated changelog — same historical-record category as this file). Left unchanged, deliberately, per the task's explicit out-of-scope list: `~SDP-Maintenance/~sdp-shared/~skill-evals/*.md` (6 files hit), `~SDP-Maintenance/~sdp-maintenance-scripts/reports/skill-script-audit-report-20260722-1400.md`, `sdp-solution-docs/log-reports/loop-metrics/log-report-loop-metrics_20260712-0943.md` and `...-1519.md`, `.sdp-solution-workflow/logging/**` (raw historical event logs — 12 files hit), and `project-scope-skill-naming-design.md` itself. Confirmed no-op, verified directly rather than assumed: `sdp-shared/scripts/tests/*.Tests.ps1` (no reference in any), `.claude/settings.local.json`, `sdp-shared/scripts/script-support/SDP-Tones.json`, `SDP-Config.json`, `QuickStart.md` (no reference in any of these four). Sibling skills `sdp-solution-coordinator` and `sdp-solution-phase-coordinator` verified untouched throughout (own identities, `SDP-Solution-Setup.json` entries, and all narrative mentions confirmed intact via a dedicated test-string regex check before trusting it against the whole repo). **This completes the 11-skill `sdp-project-*` rename effort** — the §8.3a post-completion bootstrap-doc audit (a dedicated full re-read hunting for any mention missed across the 11 per-skill passes) runs next, as a separate step. | — 
| 1.1.0 | 0 | 2026-07-24 | No version bump (skill rename, no behavior change). `sdp-state-loop` renamed to `sdp-project-state-loop` — tenth of 11 project-scoped skills renamed under the `sdp-project-*` prefix convention (see `~SDP-Maintenance/~docs/project-scope-skill-naming-design.md` §8 for the full plan, §9 for the execution log, and the prior nine 2026-07-23/24 entries below documenting skills 1-9 of 11). Renamed via `git mv` to preserve history: `.claude/skills/`, `sdp-shared/ai-skills/`. `SDP-Solution-Setup.json`: entry existed (`{ "type": "skill-pair", "name": "sdp-state-loop", "tier": "integrity" }`), `name` field updated in place — no missing-entry surprise (matches skill 9, not skill 8). The two renamed SKILL.md files' own self-references updated: L1 frontmatter `name`, both `-skillName` tone-invocation calls, the Read-path and halt-message path referencing the L2 file; the L2 file's own 28 self-references (this is the loop fire-state-machine skill — the largest single-file self-reference count of any skill renamed so far), plus **judgment call**: two `git commit -m "chore(sdp): state-loop ... infrastructure commit"` message templates (Halt Evaluation sub-step 0, both task and gate variants) updated to `project-state-loop` for identity consistency, even though they don't contain the literal `sdp-state-loop` string — treated as a shorthand self-reference, same category as a `-skillName` call. `sdp-shared/scripts/sdp-report-log-loop-metrics.ps1` checked per the task's specific flag: 3 mentions, all prose/comments (bookkeeping-skill category note, action-vocabulary comment, Idle/overhead methodology sentence) — `sdp-state-loop` is not one of the four WORKER/REVIEWER/COORDINATOR/GATE_REVIEWER roles tracked by that file's `sdp-report-log-loop-metrics-skills.json` alias config from the skill-7 bug fix, so no alias-array update applies, only the three plain-text mentions updated. Cross-references updated in every SKILL.md with a literal `sdp-state-loop` hit, carefully distinguishing from two collision-risk siblings throughout (`sdp-state-loop-start` — a literal string-prefix collision flagged in the design doc §3, confirmed a naive substring/word-boundary grep alone overcounts into it; `sdp-solution-state-loop` — no substring collision but a frequent co-mention): `sdp-shared/ai-skills/sdp-cancel-auto/SKILL.md` (2 mentions — functionally load-bearing: this skill's cron-job-matching logic literally string-matches on the skill name to find and cancel loop jobs), `.claude/skills/sdp-cancel-auto/SKILL.md` (1, frontmatter description), `sdp-shared/ai-skills/sdp-solution-state-loop/SKILL.md` (6), `.claude/skills/sdp-solution-state-loop/SKILL.md` (1), `sdp-shared/ai-skills/sdp-state-loop-start/SKILL.md` (8 — also functionally load-bearing: this skill's own loop-target string and duplicate-loop-guard `CronList` match), `sdp-shared/ai-skills/sdp-auto/SKILL.md` (7, same load-bearing cron-target/guard pattern), `sdp-shared/ai-skills/sdp-coordinator/SKILL.md` (10), `sdp-shared/ai-skills/sdp-initialize-sdp/SKILL.md` (1), `sdp-shared/ai-skills/sdp-new-concept-intake/SKILL.md` (1), `sdp-shared/ai-skills/sdp-project-gate-review/SKILL.md` (2), `sdp-shared/ai-skills/sdp-project-worker/SKILL.md` (2), `sdp-shared/ai-skills/sdp-solution-loop-prep/SKILL.md` (1). Also updated, a project instruction file not previously touched by this rename series: `.claude/rules/sdp-core-invariants.md` (2 mentions — the Loop-Owned Field Discipline invariant). Narrative docs: `sdp-shared/docs/SDP-Workspace-Setup.md` (10), `sdp-shared/docs/SDP-Project-Evolution.md` (9 — header self-declares "Status: Research — append-only," silent identifier update followed per the skill-9 precedent: the append-only header protects this doc's decisions/conclusions from silent reversal, not identifier-consistency maintenance across an unrelated rename), `README.md` (4), `sdp-shared/docs/SDP-Flowchart.md` (4), `sdp-shared/docs/SDP-Script-Authoring.md` (2). `sdp-shared/scripts/sdp-preflight.ps1` (1 comment), `sdp-shared/scripts/sdp-tone.ps1` (2 comments), `sdp-shared/scripts/sdp-create-prompt.ps1` (3 comments) — all prose/comment mentions, no logic constructs the skill name via concatenation. Bootstrap doc (`SDP_Sapient-Driven-Principles_v1.1.0.md`)'s 20 literal skill-name mentions corrected via strikethrough + 15 new dated Correction blockquotes appended immediately after each site, per Append-Only Discipline — not rewritten in place: one correction covers the 5 mentions clustered in the "Stuck-Loop Detection" section (consolidated per the same "named X N times, all instances struck through above" precedent as the 2026-07-21 gate-review correction chain), one covers 2 mentions embedded across the two 2026-07-17 loop-metrics-file corrections, and several corrections land inside pre-existing older correction chains (2026-07-10, 2026-07-17, 2026-07-17-revised) shared with skills 1-9, following the same nested-correction-inside-older-correction precedent as skills 6-9. One placement mistake caught and self-corrected during editing: an initial correction insert split a continuing sentence in the "materiality test / On trigger" section twice in a row before landing after the complete paragraph — verified by re-reading the surrounding text after each attempt. Also updated, as living tracking indexes rather than point-in-time snapshots (same treatment given to these two files in all nine prior entries): `~SDP-Maintenance/~docs/skill-catalogue.md` (2 mentions — catalogue row, Hybrid-design-session-first summary-list entry) and `~SDP-Maintenance/~docs/sdp-create-banner-adoption-tracker.md` (2 — per-skill progress row, GATE_REPAIR-narrative mention). New addition beyond the task's explicit list, disclosed: `~SDP-Maintenance/~docs/Towards self-driving codebases/self-driving-codebases-02-sdp-comparison.md` (1 mention) — updated because, unlike the three excluded docs below, it carries no self-declared closed/point-in-time status marker and reads as ongoing maintainer reference material. Confirmed excluded, checked individually rather than by generic appeal: `~SDP-Maintenance/~docs/solution-coordinator-orchestration-design.md` (Status: "Design — brainstormed and approved... through two [rounds]," dated 2026-07-19, its many mentions are closed point-in-time architectural narrative describing content already implemented elsewhere — same reasoning as skills 5/7/8/9's exclusion of this file); `~SDP-Maintenance/~docs/solution-coordinator-orchestration-plan.md` (an "Implementation Plan" checkbox task list whose ~40 mentions are copy-ready historical task content for already-implemented skills, dated 2026-07-20 era — not previously evaluated by an earlier rename in this series, checked fresh); `~SDP-Maintenance/~docs/terminal-orchestration-exploration.md` (self-declared "Status: Exploration — not a design doc, not approved for implementation," dated 2026-07-06 — checked fresh, not previously evaluated). Left unchanged, deliberately, per the task's explicit out-of-scope list (not evaluated further): `~SDP-Maintenance/~sdp-shared/~skill-evals/sdp-state-loop-eval.md`, `sdp-state-loop-start-eval.md`, `sdp-auto-eval.md`, `sdp-cancel-auto-eval.md`; `~SDP-Maintenance/~sdp-maintenance-scripts/reports/skill-script-audit-report-20260722-1400.md`; `sdp-solution-docs/log-reports/loop-metrics/log-report-loop-metrics_20260712-0943.md` and `...-1519.md`; and `project-scope-skill-naming-design.md` itself. Confirmed no-op, verified directly rather than assumed: `sdp-shared/scripts/tests/sdp-preflight.Tests.ps1`, `sdp-create-prompt.Tests.ps1`, `sdp-github.Tests.ps1` (no reference in any); `.claude/settings.local.json` and `sdp-shared/scripts/script-support/SDP-Tones.json` (no exact-match reference — the only hit in `SDP-Tones.json` is the unrelated sibling `sdp-solution-state-loop`); `sdp-shared/scripts/script-support/sdp-report-log-loop-metrics-skills.json` (no reference — `sdp-state-loop` is not a tracked role). **Not touched, flagged instead:** `SDP-Config.json` (3 prose "notes" mentions of `sdp-state-loop`) and `sdp-shared/docs/SDP-Tone-Notifications.md` (narrative mentions) were both explicitly identified by the orchestrating session as carrying unrelated, currently-dirty concurrent-work changes outside this task's scope — left untouched per that explicit instruction rather than risk entangling with those changes; both are genuine remaining `sdp-state-loop` mentions the orchestrating session or a follow-up pass should address once that concurrent work is reconciled. Sibling skills `sdp-solution-state-loop` and `sdp-state-loop-start` verified untouched throughout (own identities, `SDP-Solution-Setup.json`/`SDP-Tones.json` entries, and all narrative mentions confirmed intact — including the literal string-prefix collision risk with `sdp-state-loop-start`, verified via a dedicated regex test before trusting it against the whole repo). |
| 1.1.0 | 0 | 2026-07-24 | No version bump (skill rename, no behavior change). `sdp-create-prompt` renamed to `sdp-project-create-prompt` — ninth of 11 project-scoped skills renamed under the `sdp-project-*` prefix convention (see `~SDP-Maintenance/~docs/project-scope-skill-naming-design.md` §8 for the full plan, §9 for the execution log, and the prior eight 2026-07-23/24 entries below documenting skills 1-8 of 11). Renamed via `git mv` to preserve history: `.claude/skills/`, `sdp-shared/ai-skills/`. `SDP-Solution-Setup.json`: entry existed (`{ "type": "skill-pair", "name": "sdp-create-prompt", "tier": "integrity" }`), `name` field updated in place — no missing-entry surprise this time (unlike skill 8). The two renamed SKILL.md files' own self-references updated: L1 frontmatter `name`, both tone-invocation `-skillName` calls, the Read-path and halt-message path referencing the L2 file; the L2 file's own single self-reference (the halted-workflow error banner's `/sdp-create-prompt again` slash-command text). `sdp-shared/scripts/sdp-create-prompt.ps1` (the skill's own backing script, filename unchanged): `.SYNOPSIS` comment updated, plus **judgment call** — the three `_meta.skill = "sdp-create-prompt"` literal metadata values (written into every temp/tracking JSON file) were also updated to `"sdp-project-create-prompt"`, treated as the same class of self-referencing string the task called out via the `.SYNOPSIS` example. By contrast the script's actual generated-artifact filename convention (`phase{N}-sdp-create-prompt-{timestamp}.json`, `sdp-create-prompt-tracking.json`) was deliberately left unchanged — renaming that would be a real behavior/output-naming change (different files produced on disk), not a self-reference update; nothing outside the script and its own test depends on the specific string (verified by grep); and it mirrors the same non-renaming principle already applied to the `.ps1` filename itself. `sdp-shared/docs/SDP-Project-Evolution.md`'s matching example JSON (`"skill": "sdp-create-prompt"`) was updated to keep the doc's example consistent with the script's new output; the doc's matching temp-filename example (`phase3-sdp-create-prompt-202619061430.json`) was left as-is to match the script's actual (unchanged) behavior. `sdp-shared/scripts/sdp-run-prompt.ps1` (3 mentions — literal `/sdp-create-prompt` slash-invocation strings inside error-message text, matching the precedent set for `/sdp-worker`/`/sdp-reviewer`/`/sdp-gate-review` in prior entries) and `sdp-shared/scripts/sdp-report-log-loop-metrics.ps1` (2 mentions — a comment and one methodology-text sentence; note `sdp-create-prompt` is not one of the four WORKER/REVIEWER/COORDINATOR/GATE_REVIEWER roles tracked by that file's `sdp-report-log-loop-metrics-skills.json` alias config from the skill-7 bug fix, so no alias-array update applies here, only the plain-text mentions). Cross-references checked in every SKILL.md with a literal `sdp-create-prompt` hit and updated only where the mention named the *skill* rather than the `.ps1` *script* (careful boundary-drawing, since this skill has an unusually script-heavy footprint): `sdp-shared/ai-skills/sdp-state-loop/SKILL.md` (8 of 12 mentions — the other 4 are `sdp-create-prompt.ps1` script references, left unchanged) and `sdp-shared/ai-skills/sdp-project-run-prompt/SKILL.md` (1 mention). Checked and confirmed no-op (all hits are `.ps1` script-file references, not skill-name references): `sdp-shared/ai-skills/sdp-standards-setup/SKILL.md`, `sdp-shared/ai-skills/sdp-coordinator/SKILL.md`, `sdp-shared/ai-skills/sdp-auto/SKILL.md`. Narrative docs: `sdp-shared/docs/SDP-Project-Evolution.md` (8 of 16 mentions — the other 8 are script/artifact-filename references left unchanged per the judgment call above; header self-declares "Status: Research — append-only," but the skill-7 rename commit already established silent identifier updates as the precedent for this file's illustrative tables/examples, followed here for consistency), `sdp-shared/docs/SDP-Script-Authoring.md` (2 of 9 mentions — the rest are `.ps1` script references), `sdp-shared/docs/SDP-Workspace-Setup.md` (1 of 6 mentions — a skill-pair enumeration list; the rest are script/test-filename references), `sdp-shared/docs/SDP-Flowchart.md` (1 mention), `README.md` (3 of 4 mentions — Hybrid-classification-table prose + row, Purpose-table row; the fourth, in the Script Boundaries table, is a `.ps1` filename reference left unchanged). Also updated, as living tracking indexes rather than point-in-time snapshots (same treatment given to these two files in all eight prior entries): `~SDP-Maintenance/~docs/skill-catalogue.md` (2 mentions — catalogue row, `Extend-Existing-Script — Applied` summary-list entry) and `~SDP-Maintenance/~docs/sdp-create-banner-adoption-tracker.md` (1 — per-skill progress row). Bootstrap doc (`SDP_Sapient-Driven-Principles_v1.1.0.md`)'s nine literal skill-name mentions corrected via strikethrough + eight new dated Correction blockquotes appended immediately after each site, per Append-Only Discipline — not rewritten in place (one correction covers two adjacent mentions in the `sdp-docs/00_prompt.txt` subsection; three corrections land inside the pre-existing "SKILLS CHECK superseded" correction chains shared with skills 1-8, following the same nested-correction-inside-older-correction precedent as skills 6-8). Confirmed excluded, checked individually rather than by generic appeal: `~SDP-Maintenance/~docs/solution-coordinator-orchestration-design.md` — status "Design — brainstormed and approved... through two [rounds]," dated 2026-07-19, its one mention (an inventory of solution-level skill companions compiled that day) is closed point-in-time architectural narrative, already excluded for the same reason during skills 5/7/8's rename passes. Left unchanged, deliberately, per the task's explicit out-of-scope list (not evaluated further): `~SDP-Maintenance/~sdp-shared/~skill-evals/sdp-create-prompt-eval.md`, `sdp-gate-review-eval.md`, `sdp-run-prompt-eval.md`, `sdp-solution-create-prompt-eval.md`, and `sdp-state-loop-eval.md`; `~SDP-Maintenance/~sdp-maintenance-scripts/reports/skill-script-audit-report-20260722-1400.md`; `sdp-solution-docs/log-reports/loop-metrics/log-report-loop-metrics_20260712-0943.md` and `...-1519.md`; `.sdp-solution-workflow/logging/**/*.jsonl`; and `project-scope-skill-naming-design.md` itself. Confirmed no-op, verified directly rather than assumed: `sdp-shared/scripts/tests/sdp-create-prompt.Tests.ps1` (its one skill-name-adjacent fixture string already reads `sdp-project-worker`, from skill 7's rename — not this skill), `.claude/settings.local.json`, and `sdp-shared/scripts/script-support/SDP-Tones.json` — no exact-match reference to this skill in any of the three (the only hit in each is the unrelated sibling `sdp-solution-create-prompt`). Sibling skill `sdp-solution-create-prompt` verified untouched throughout (its own `SDP-Solution-Setup.json`/`settings.local.json`/`SDP-Tones.json` entries and all narrative mentions confirmed intact and unmodified). |
| 1.1.0 | 0 | 2026-07-24 | No version bump (skill rename, no behavior change). `sdp-gate-review` renamed to `sdp-project-gate-review` — eighth of 11 project-scoped skills renamed under the `sdp-project-*` prefix convention (mirrors the existing `sdp-solution-*` convention; see `~SDP-Maintenance/~docs/project-scope-skill-naming-design.md` §8 for the full plan, §9 for the execution log, and the prior seven 2026-07-23/24 entries below documenting skills 1-7 of 11). Renamed via `git mv` to preserve history: `.claude/skills/`, `sdp-shared/ai-skills/`. `SDP-Solution-Setup.json`: **judgment call, flagged, and corrected on independent review** — unlike every other one of the 10 already-processed project-only skills, no `skill-pair` entry for `sdp-gate-review` existed in this manifest at the time this rename began. The edit agent's own report characterized this as a pre-existing gap unrelated to any rename; independent review traced the actual history via `git log -p` and found that characterization wrong — the entry was present and continuously registered from the manifest's 2026-07-18 inception until commit `d32bc1a` (2026-07-21, "close the solution-scoped phases 1-7 WORKER/REVIEWER/GATE_REVIEWER dispatch gap") deleted it, apparently as an unintentional side effect of that commit's manifest restructuring (its own commit message discusses removing this skill's `-scope project|solution` parameter, never mentions dropping its preflight registration). So this skill's L1/L2 pair went unvalidated by `sdp-preflight.ps1` for roughly 3 days / 8 commits (2026-07-21 through today), not "since inception." Added a fresh `sdp-project-gate-review` entry in the position this skill would have sorted to alphabetically by its old name (between `sdp-evaluate-skill` and `sdp-initialize-sdp`, matching this file's established ordering convention, and matching where the original entry sat before its 2026-07-21 deletion) — the remediation itself was correct either way; only the characterization of the gap's origin needed correcting. The two renamed SKILL.md files' own self-references updated: L1 frontmatter `name`, both tone-invocation `-skillName` calls, the Read-path and halt-message path referencing the L2 file; the L2 file's own single self-reference (the Step 9a "Why this step exists" rationale note, alongside its already-renamed `sdp-project-reviewer`/`sdp-project-worker` neighbors). `sdp-shared/scripts/script-support/sdp-report-log-loop-metrics-skills.json`: appended `"sdp-project-gate-review"` to the `GATE_REVIEWER` role's `aliases` array (now `["sdp-gate-review", "sdp-project-gate-review"]`), per the file's own maintenance rule — the bug this file itself was created to fix (see the entry below documenting `sdp-report-log-loop-metrics.ps1`) would otherwise recur for this skill's historical tone events. Three project-scoped helper scripts' own `.SYNOPSIS`/comment self-references updated, filenames unchanged (matching the precedent set for `sdp-run-prompt.ps1`/`sdp-create-prompt.ps1` in prior entries): `sdp-shared/scripts/sdp-gate-review-setup.ps1` (2 mentions), `sdp-shared/scripts/sdp-gate-review-gpg-check.ps1` (4 mentions), `sdp-shared/scripts/sdp-gate-review-finalize.ps1` (2 mentions); the three solution-scoped sibling scripts (`sdp-solution-phase-gate-review-*.ps1`) were checked directly and found to reference only script filenames, not the skill name — no edit needed there. `sdp-shared/scripts/sdp-create-prompt.ps1` (2 mentions — literal `/sdp-gate-review` slash-invocation strings inside generated GATE_REVIEWER dispatch prose). Cross-references updated in 11 other SKILL.md files: `sdp-shared/ai-skills/sdp-solution-phase-gate-review/SKILL.md` (6 mentions) and `.claude/skills/sdp-solution-phase-gate-review/SKILL.md` (1 — description field), `sdp-shared/ai-skills/sdp-coordinator/SKILL.md` (3), `sdp-shared/ai-skills/sdp-new-concept-intake/SKILL.md` (1), `sdp-shared/ai-skills/sdp-workspace-setup/SKILL.md` (1), `sdp-shared/ai-skills/sdp-standards-setup/SKILL.md` (1), `sdp-shared/ai-skills/sdp-solution-run-prompt/SKILL.md` (1), `sdp-shared/ai-skills/sdp-solution-create-prompt/SKILL.md` (1), `sdp-shared/ai-skills/sdp-solution-coordinator/SKILL.md` (1), `sdp-shared/ai-skills/sdp-project-loop-prep/SKILL.md` (1), `sdp-shared/ai-skills/sdp-solution-phase-reviewer/SKILL.md` (1). Narrative docs: `README.md` (5 mentions — Hybrid-classification table row + prose, Purpose-table row, `sdp-solution-phase-gate-review` companion-row prose, Phase-7-gate-narrative prose), `sdp-shared/docs/SDP-Script-Authoring.md` (1), `sdp-shared/docs/SDP-Workspace-Setup.md` (1 — skill-pair enumeration list). `sdp-shared/docs/SDP-Project-Evolution.md` checked directly — its one mention is script-filenames only (`sdp-gate-review-finalize.ps1`/`-gpg-check.ps1`/`-setup.ps1`), no edit needed. `sdp-shared/docs/SDP-Tone-Notifications.md` checked directly — its one mention is also a script-filename only (no edit needed regardless of scope), and this file is additionally out of scope per this task's explicit unrelated-concurrent-work note, left untouched either way. Also updated, as living tracking indexes rather than point-in-time snapshots (same treatment given to these two files in all seven prior entries): `~SDP-Maintenance/~docs/skill-catalogue.md` (2 mentions — catalogue row, `Hybrid — Applied` summary-list entry; note this file does **not** duplicate README's Hybrid-classification/Skills-Inventory tables as first assumed mid-pass — confirmed by direct diff that only these 2 lines actually exist in this 91-line file) and `~SDP-Maintenance/~docs/sdp-create-banner-adoption-tracker.md` (1 — per-skill progress row only; its Gap-audit narrative line mentioning "sibling skill `sdp-gate-review`" inside `sdp-solution-coordinator`'s own row is dated 2026-07-17 point-in-time narrative, left unchanged per the exact precedent already established for equivalent mentions during skills 1-7's rename passes). Bootstrap doc (`SDP_Sapient-Driven-Principles_v1.1.0.md`)'s ten literal mentions corrected via strikethrough + five new dated Correction blockquotes appended immediately after each site, per Append-Only Discipline — not rewritten in place, including the cases where a mention sat inside a pre-existing Correction blockquote from an earlier pass (the existing 2026-07-21 gate-review-scope Correction, four mentions, one consolidated new Correction; the existing 2026-07-07 field-name Correction plus its adjacent item-5 prose, three mentions total, one consolidated new Correction): the Phase-Readiness-regression-mechanism paragraph (1 mention, its own new Correction), the COORDINATOR procedure's Phase Gate dispatch bullet (1 mention, its own new Correction), and the "Phase Gate Procedure" section's item 2 (1 mention, its own new Correction). One remaining bootstrap-doc hit (the existing 2026-07-17 `sdp-workflow-log.ps1` Addition blockquote's `sdp-gate-review-finalize.ps1` mention) is a script filename, left unchanged. Left unchanged, deliberately: `~SDP-Maintenance/~sdp-shared/~skill-evals/sdp-gate-review-eval.md`, `sdp-evaluate-skill-eval.md`, `sdp-loop-prep-eval.md`, `sdp-phase-rightsizing-check-eval.md`, and `sdp-solution-reviewer-eval.md`, `~SDP-Maintenance/~sdp-maintenance-scripts/reports/skill-script-audit-report-20260722-1400.md` (point-in-time historical records, immutable snapshots of what was true when written — same convention already applied to every prior rename entry in this file); `sdp-solution-docs/log-reports/loop-metrics/log-report-loop-metrics_20260712-1519.md` and `...-0943.md` (generated report snapshots); `.sdp-solution-workflow/logging/**/*.jsonl` (raw historical event logs); and `project-scope-skill-naming-design.md` itself (a planning record of this very rename, whose own §9 tracking table is owned by the orchestrating session, not this edit pass). Also judged out of scope, on individual review of each file's own status header and the specific sentence containing the hit (not a generic same-class appeal): `~SDP-Maintenance/~docs/solution-phase-dispatch-gap-design.md` — status "Implemented" (Tier 1/2 complete), dated 2026-07-21, its mentions are point-in-time architectural rationale describing the pre-2026-07-21 `-scope`-parameter design, since superseded, same file/reasoning already applied in prior entries; `~SDP-Maintenance/~docs/solution-coordinator-orchestration-design.md` — status "Design ... No skill file has been changed yet," dated 2026-07-19, same class of closed architectural record, already excluded for this same reason during prior entries' renames; `~SDP-Maintenance/~docs/solution-coordinator-orchestration-plan.md` — an implementation plan referencing the certified-complete design doc above, dated, its Task 6/7/11 mentions record a past task list already executed via the live skill/script files this entry directly edited, not living reference guidance; `~SDP-Maintenance/~docs/phase-pipeline-expansion-plan.md` — an implementation plan approved 2026-07-18 for the already-shipped 5→7 phase expansion, dated, its mentions are a closed historical task record. Also excluded per the task's explicit unrelated-concurrent-work note (not touched, not evaluated further): `~SDP-Maintenance/~docs/material-decision-escalation-design.md`. Confirmed no-op, verified directly rather than assumed: `sdp-shared/scripts/tests/*.Tests.ps1`, `.claude/settings.local.json`, and `sdp-shared/scripts/script-support/SDP-Tones.json` — no exact-match reference to this skill in any of the three. |
| 1.1.0 | 0 | 2026-07-24 | No version bump (bug fix, `sdp-report-log-loop-metrics.ps1` only). Found during the `sdp-worker`/`sdp-reviewer` rename review passes (see `~SDP-Maintenance/~docs/project-scope-skill-naming-design.md` §9): the script identified "productive" WORKER/REVIEWER/COORDINATOR/GATE_REVIEWER work intervals by hardcoding the CURRENT literal skill name for each role directly in its matching logic (`$productiveSkills = @("sdp-project-worker", "sdp-project-reviewer", "sdp-coordinator", "sdp-gate-review")`, plus two `-eq "sdp-coordinator"` sites for halt-resolution detection and two `-eq "sdp-project-worker"` sites for user-interrupted-span/primary-interval lookups). Every time one of these four skills got renamed, whoever did the rename swapped the old literal for the new one in these five sites — correct for future tone-log events, but a swap, not a union: re-running this report against a historical date from *before* that skill's rename silently stopped recognizing that skill's old-named tone events, dropping them out of Productive-time bucketing entirely (reclassified as Idle) with no error or warning. Confirmed against real on-disk data: `.sdp-solution-workflow/logging/loop-logs/loop-metrics-20260710.jsonl`, `-11.jsonl`, and `-12.jsonl` contain 9/8, 17/12, and 4/6 real `"skillName":"sdp-worker"`/`"sdp-reviewer"` tone events respectively (zero `"sdp-project-worker"`/`"sdp-project-reviewer"` events in any of the three), which the script — as it stood immediately after those two skills' renames — no longer matched against its own hardcoded `$productiveSkills` list. Root cause: the fix for a rename was applied as a literal find-and-replace at each comparison site instead of as an accumulating alias list. Fix: externalized the four roles' skill-name lists (plus the `-OffHoursThresholdMinutes` default and the separate 15-minute unlogged-gap anomaly threshold, both previously hardcoded in the script body) into a new companion config file, `sdp-shared/scripts/script-support/sdp-report-log-loop-metrics-skills.json` — read at startup, following the same `script-support/` convention as `SDP-Tones.json`/`sdp-hook-log-tools.json`. Each of the four roles (`WORKER`/`REVIEWER`/`COORDINATOR`/`GATE_REVIEWER`) carries every literal name it has ever been logged under, oldest first, current name last; the script builds an alias-to-canonical lookup from this and normalizes every parsed log entry's `skillName` field to its canonical current name once, immediately after parsing and before any interval computation, bucketing, or comparison — so a historical `sdp-worker` event and a current `sdp-project-worker` event now merge into the same continuous Productive-time bucket instead of one of them silently falling out of it. The five original hardcoded-match sites now compare against the config's canonical names instead of literals. A future rename of one of these four skills is now "append the new name to that role's array in the JSON" — no script code change required. Verified directly against the three affected real dates above (not synthetic data): re-running the fixed script with `-JsonlPath`/`-Date` against each of 2026-07-10, -11, and -12 shows Productive time increasing (2026-07-10: 1h 38m → 2h 39m; 2026-07-11: 3h 51m → 5h 10m; 2026-07-12: 34m → 58m) with Idle decreasing by the same margin in each case, confirming the previously-dropped `sdp-worker`/`sdp-reviewer` intervals now correctly bucket as Productive under the current `sdp-project-worker`/`sdp-project-reviewer` canonical grouping. `sdp-shared/docs/SDP-Script-Authoring.md`'s worked example for this script updated with a new annotated pattern describing the config-driven alias-normalization approach. `SDP-Solution-Setup.json` and `SDP-Workspace-Setup.md` (folder diagram + a new Create/Copy checklist item, mirroring the existing `sdp-hook-log-tools.json` entry) updated to register the new config file the same way its `script-support/` siblings are registered. No Pester test file exists for this script or any other report-family script (`sdp-report-log-hook-metrics.ps1`, `sdp-report-log-workflow-metrics.ps1`, `sdp-report-log-combined-metrics.ps1` are likewise untested), so none was added, consistent with that existing convention. |
| 1.1.0 | 0 | 2026-07-24 | No version bump (skill rename, no behavior change). `sdp-worker` renamed to `sdp-project-worker` — seventh of 11 project-scoped skills renamed under the `sdp-project-*` prefix convention (mirrors the existing `sdp-solution-*` convention; see `~SDP-Maintenance/~docs/project-scope-skill-naming-design.md` §8 for the full plan, §9 for the execution log, and the prior six 2026-07-23/24 entries below documenting skills 1-6 of 11). Renamed via `git mv` to preserve history: `.claude/skills/`, `sdp-shared/ai-skills/`. Every literal-identifier reference updated to match: `SDP-Solution-Setup.json` (`skill-pair` entry), the L1 SKILL.md's own self-references (frontmatter `name`, both tone-invocation `-skillName` calls, the Read-path and halt-message path referencing the L2 file) — the L2 file itself carries no self-reference to its own name (confirmed via direct grep, zero hits). Sibling skill `sdp-solution-worker` (already correctly solution-scoped) confirmed untouched throughout — its own identity, and every legitimate cross-reference to it, verified unmodified by a separate word-boundary grep; same confirmation for `sdp-solution-phase-worker` and `sdp-project-pre-work-verify`. Cross-references updated in 14 other SKILL.md files: `sdp-shared/ai-skills/sdp-solution-phase-worker/SKILL.md` (5 mentions), `sdp-shared/ai-skills/sdp-solution-worker/SKILL.md` (3), `.claude/skills/sdp-solution-phase-worker/SKILL.md` (2 — description field, prose), `sdp-shared/ai-skills/sdp-solution-coordinator/SKILL.md` (2), `sdp-shared/ai-skills/sdp-standards-setup/SKILL.md` (2), `sdp-shared/ai-skills/sdp-coordinator/SKILL.md` (1), `sdp-shared/ai-skills/sdp-gate-review/SKILL.md` (1), `sdp-shared/ai-skills/sdp-project-loop-prep/SKILL.md` (1), `sdp-shared/ai-skills/sdp-project-run-prompt/SKILL.md` (1), `sdp-shared/ai-skills/sdp-report-log-loop-metrics/SKILL.md` (1), `sdp-shared/ai-skills/sdp-solution-create-prompt/SKILL.md` (1), `sdp-shared/ai-skills/sdp-solution-run-prompt/SKILL.md` (1), `sdp-shared/ai-skills/sdp-solution-state-loop/SKILL.md` (1), `sdp-shared/ai-skills/sdp-workspace-setup/SKILL.md` (1). Narrative docs: `README.md` (5 mentions — Architectural Boundary prose, two skill-table rows, a Purpose-table row, a PowerShell-scripts-table row), `sdp-shared/docs/SDP-Workspace-Setup.md` (3 — skill-pair enumeration list, two logging-narrative repo-tree comments), `sdp-shared/docs/SDP-Project-Evolution.md` (2 — an LLM-required table row and a generated-sentinel JSON example), `sdp-shared/docs/SDP-Script-Authoring.md` (1). Scripts: `sdp-shared/scripts/sdp-report-log-loop-metrics.ps1` (6 literal mentions — a `$productiveSkills` array literal used for actual tone-event matching logic, two code comments, two `-eq "sdp-worker"` filter conditions that are logic-critical since real tone events now log under the new name, and the generated Methodology here-string), `sdp-shared/scripts/sdp-create-prompt.ps1` (1 — a generated-prompt narrative string), `sdp-shared/scripts/sdp-solution-coordinator.ps1` and `sdp-shared/scripts/sdp-solution-phase-coordinator.ps1` (1 each — an inline dispatch-instruction ternary), `sdp-shared/scripts/sdp-tone.ps1` (1 — a `.PARAMETER` doc-comment example). Test file `sdp-shared/scripts/tests/sdp-create-prompt.Tests.ps1` (1 mention — a halt-reason fixture string using the skill name as an arbitrary example, not behavior keyed to this skill's actual identity, but renamed anyway for literal-string consistency, matching the precedent set for `sdp-read-docs`/`sdp-run-prompt` fixture strings in the fifth entry below). No hit found, confirmed no-op rather than assumed: `sdp-shared/scripts/tests/sdp-preflight.Tests.ps1`, `.claude/settings.local.json`, and `sdp-shared/scripts/script-support/SDP-Tones.json`. Also updated, as living tracking indexes rather than point-in-time snapshots (same treatment given to these same two files in all six prior entries): `~SDP-Maintenance/~docs/skill-catalogue.md` (catalogue row, LLM-Only list) and `~SDP-Maintenance/~docs/sdp-create-banner-adoption-tracker.md` (per-skill progress row). Bootstrap doc (`SDP_Sapient-Driven-Principles_v1.1.0.md`)'s thirteen literal mentions (the SP-vs-SDP Terminology note; the WORKER Invocation Reference staleness notice; both mentions inside the existing 2026-07-21 gate-review-scope Correction blockquote; the three duplicate already-superseded WORKER-session/REVIEWER-session/GATE_REVIEWER-session SKILLS CHECK blocks, each already carrying `sdp-pre-work-verify`/`sdp-run-prompt`/`sdp-read-docs`/`sdp-reviewer` Corrections from prior entries — a further, separate dated Correction appended to each for this skill; the WORKER-dispatch Task Instruction template and notify-user message, sharing one new Correction; the solution-coordinator fan-out step; the existing 2026-07-17 `sdp-workflow-log.ps1` Addition blockquote; the existing 2026-07-17 Halt Behavior Contract Correction blockquote; and the Key-gates-before-first-WORKER-dispatch checklist bullet) corrected via strikethrough + a new dated Correction appended immediately after each, per Append-Only Discipline — not rewritten in place, including the cases where the mention sat inside a pre-existing Correction/Addition blockquote from an earlier pass (those blockquotes are themselves now-existing document content, so the same strikethrough-plus-new-dated-note treatment applies rather than a silent in-place edit). Left unchanged, deliberately: `~SDP-Maintenance/~sdp-shared/~skill-evals/sdp-worker-eval.md`, `sdp-run-prompt-eval.md`, and `sdp-solution-worker-eval.md`, `~SDP-Maintenance/~sdp-maintenance-scripts/reports/skill-script-audit-report-20260722-1400.md` (point-in-time historical records, immutable snapshots of what was true when written — same convention already applied to every prior rename/relocation entry in this file); `sdp-solution-docs/log-reports/loop-metrics/log-report-loop-metrics_20260712-1519.md` and `...-0943.md` (generated report snapshots); `.sdp-solution-workflow/logging/**/*.jsonl` (raw historical event logs); and `project-scope-skill-naming-design.md` itself (a planning record of this very rename, whose own §9 tracking table is owned by the orchestrating session, not this edit pass). Also judged out of scope, on individual review of each file's own status header and the specific sentence containing the hit (not a generic same-class appeal): `~SDP-Maintenance/~docs/phase-1-3-interactive-capture-design.md` — status "Implemented; one same-day follow-up," dated 2026-07-21, its one mention is a closed architectural note that project-level `sdp-coordinator`/`sdp-worker` were untouched by that design; `~SDP-Maintenance/~docs/solution-phase-dispatch-gap-design.md` — status "Implemented" (Tier 1/2 complete), dated 2026-07-21, its several mentions are point-in-time architectural rationale for why `sdp-worker`/`sdp-reviewer` were left unmodified when the solution-scoped WORKER/REVIEWER/GATE_REVIEWER dispatch gap was closed — same file, same reasoning already applied in the fourth and sixth entries below; `~SDP-Maintenance/~docs/solution-coordinator-orchestration-design.md` — status "Design ... No skill file has been changed yet," dated 2026-07-19, same class of closed architectural record, already excluded for this same reason during prior entries' renames; `~SDP-Maintenance/~docs/solution-coordinator-orchestration-plan.md` — an implementation plan referencing the certified-complete design doc above, dated, its several mentions are Global-Constraints boundary notes and architectural-option rationale recording a past decision, not living reference guidance. Also excluded per the task's explicit unrelated-concurrent-work note (not touched, not evaluated further): `~SDP-Maintenance/~docs/material-decision-escalation-design.md`. |
| 1.1.0 | 0 | 2026-07-24 | No version bump (skill rename, no behavior change). `sdp-reviewer` renamed to `sdp-project-reviewer` — sixth of 11 project-scoped skills renamed under the `sdp-project-*` prefix convention (mirrors the existing `sdp-solution-*` convention; see `~SDP-Maintenance/~docs/project-scope-skill-naming-design.md` §8 for the full plan, §9 for the execution log, and the prior five 2026-07-23 entries below documenting skills 1-5 of 11). Renamed via `git mv` to preserve history: `.claude/skills/`, `sdp-shared/ai-skills/`. Every literal-identifier reference updated to match: `SDP-Solution-Setup.json` (`skill-pair` entry), the L1 SKILL.md's own self-references (frontmatter `name`, both tone-invocation `-skillName` calls, the Read-path and halt-message path referencing the L2 file) — the L2 file itself carries no self-reference to its own name. Sibling skill `sdp-solution-reviewer` (already correctly solution-scoped) confirmed untouched throughout — its own identity, and every legitimate mention of it, verified unmodified by a separate word-boundary grep. Cross-references updated in 14 other SKILL.md files: `.claude/skills/sdp-solution-phase-reviewer/SKILL.md` (2 mentions — description field, prose), `sdp-shared/ai-skills/sdp-solution-phase-reviewer/SKILL.md` (5 mentions), `sdp-shared/ai-skills/sdp-solution-reviewer/SKILL.md` (5 mentions), `sdp-shared/ai-skills/sdp-solution-create-prompt/SKILL.md` (1), `sdp-shared/ai-skills/sdp-solution-state-loop/SKILL.md` (1), `sdp-shared/ai-skills/sdp-solution-run-prompt/SKILL.md` (1), `sdp-shared/ai-skills/sdp-solution-coordinator/SKILL.md` (2), `sdp-shared/ai-skills/sdp-standards-setup/SKILL.md` (1), `sdp-shared/ai-skills/sdp-project-run-prompt/SKILL.md` (1), `sdp-shared/ai-skills/sdp-coordinator/SKILL.md` (1), `sdp-shared/ai-skills/sdp-gate-review/SKILL.md` (2), `sdp-shared/ai-skills/sdp-workspace-setup/SKILL.md` (1), `sdp-shared/ai-skills/sdp-project-loop-prep/SKILL.md` (1), `sdp-shared/ai-skills/sdp-report-log-loop-metrics/SKILL.md` (1). Narrative docs: `README.md` (5 mentions — Architectural Boundary prose, two skill-table rows, a Purpose-table row, a PowerShell-scripts-table row), `sdp-shared/docs/SDP-Workspace-Setup.md` (3 — skill-pair enumeration list, two logging-narrative repo-tree comments), `sdp-shared/docs/SDP-Tone-Notifications.md` (1 — `milestone.task_verified` row), `sdp-shared/docs/SDP-Script-Authoring.md` (1), `sdp-shared/docs/SDP-Project-Evolution.md` (1 — LLM-required table row). Scripts: `sdp-shared/scripts/sdp-report-log-loop-metrics.ps1` (3 literal mentions — a `$productiveSkills` array literal, a code-comment, and the generated Methodology here-string), `sdp-shared/scripts/sdp-solution-phase-coordinator.ps1` and `sdp-shared/scripts/sdp-solution-coordinator.ps1` (1 each — an inline dispatch-instruction ternary), `sdp-shared/scripts/sdp-create-prompt.ps1` (1 — a generated-prompt narrative string). Also updated, as living tracking indexes rather than point-in-time snapshots (same treatment given to these same two files in all five prior entries): `~SDP-Maintenance/~docs/skill-catalogue.md` (catalogue row, LLM-Only list) and `~SDP-Maintenance/~docs/sdp-create-banner-adoption-tracker.md` (per-skill progress row). Bootstrap doc (`SDP_Sapient-Driven-Principles_v1.1.0.md`)'s twelve literal mentions (the SP-vs-SDP Terminology note; both mentions inside the existing 2026-07-21 gate-review-scope Correction blockquote; the three duplicate already-superseded WORKER-session/REVIEWER-session/GATE_REVIEWER-session SKILLS CHECK blocks, each already carrying `sdp-pre-work-verify`/`sdp-run-prompt`/`sdp-read-docs` Corrections from prior entries — a further, separate dated Correction appended to each for this skill; the REVIEWER-dispatch Task Instruction template and notify-user message, sharing one new Correction; the solution-coordinator fan-out step; the existing 2026-07-17 `sdp-workflow-log.ps1` Addition blockquote; the existing 2026-07-17 Halt Behavior Contract Correction blockquote; and the Key-gates-before-first-WORKER-dispatch checklist bullet) corrected via strikethrough + a new dated Correction appended immediately after each, per Append-Only Discipline — not rewritten in place, including the two cases where the mention sat inside a pre-existing Correction/Addition blockquote from an earlier pass (those blockquotes are themselves now-existing document content, so the same strikethrough-plus-new-dated-note treatment applies rather than a silent in-place edit). Left unchanged, deliberately: `~SDP-Maintenance/~sdp-shared/~skill-evals/sdp-reviewer-eval.md` and `sdp-solution-reviewer-eval.md`, `~SDP-Maintenance/~sdp-maintenance-scripts/reports/skill-script-audit-report-20260722-1400.md` (point-in-time historical records, immutable snapshots of what was true when written — same convention already applied to every prior rename/relocation entry in this file); `sdp-solution-docs/log-reports/loop-metrics/log-report-loop-metrics_20260712-1519.md` and `...-0943.md` (generated report snapshots); `.sdp-solution-workflow/logging/**/*.jsonl` (raw historical event logs); and `project-scope-skill-naming-design.md` itself (a planning record of this very rename, whose own §9 tracking table is owned by the orchestrating session, not this edit pass). Also judged out of scope, on individual review of each file's own status header and the specific sentence containing the hit (not a generic same-class appeal): `~SDP-Maintenance/~docs/solution-phase-dispatch-gap-design.md` — status "Implemented," dated 2026-07-21, its several mentions are a point-in-time architectural rationale for why `sdp-worker`/`sdp-reviewer` were left unmodified when the solution-scoped WORKER/REVIEWER/GATE_REVIEWER dispatch gap was closed; `~SDP-Maintenance/~docs/solution-coordinator-orchestration-design.md` — status "Design — approved... No skill file has been changed yet," dated 2026-07-19, same class of closed architectural record, already excluded for this same reason during a prior entry's rename; `~SDP-Maintenance/~docs/solution-coordinator-orchestration-plan.md` — an implementation plan referencing the certified-complete design doc above, dated, its one mention is a Global-Constraints boundary note ("never invoke `sdp-worker`/`sdp-reviewer` directly") recording a past architectural decision, not living reference guidance. Also excluded per the task's explicit unrelated-concurrent-work note (not touched, not evaluated further): `~SDP-Maintenance/~docs/material-decision-escalation-design.md`. Confirmed no-op, verified directly rather than assumed: `sdp-shared/scripts/tests/*.Tests.ps1`, `.claude/settings.local.json`, and `sdp-shared/scripts/script-support/SDP-Tones.json` — no exact-match reference to this skill in any of the three. |
| 1.1.0 | 0 | 2026-07-23 | No version bump (skill rename, no behavior change). `sdp-read-docs` renamed to `sdp-project-read-docs` — fifth of 11 project-scoped skills renamed under the `sdp-project-*` prefix convention (mirrors the existing `sdp-solution-*` convention; see `~SDP-Maintenance/~docs/project-scope-skill-naming-design.md` §8 for the full plan, §9 for the execution log, and the prior four 2026-07-23 entries below documenting skills 1-4 of 11). Renamed via `git mv` to preserve history: `.claude/skills/`, `sdp-shared/ai-skills/`. Every literal-identifier reference updated to match: `SDP-Solution-Setup.json` (`skill-pair` entry), the two SKILL.md files' own self-references (L1 frontmatter `name`, the `--no-tone` invocation example, both tone-invocation `-skillName` calls, the L1 Read-path and halt-message path referencing the L2 file, the L2 Step 1 invocation-argument example, and the L2 note pointing back at the L1 file), cross-references in `sdp-shared/ai-skills/sdp-auto/SKILL.md` (7 mentions — Step 0's session-start-hook wait logic ×6, Constraints bullet ×1), `sdp-shared/ai-skills/sdp-project-loop-prep/SKILL.md` (1 mention), `sdp-shared/ai-skills/sdp-state-loop-start/SKILL.md` (7 mentions — same Step 0 wait-logic pattern as `sdp-auto`), and `sdp-shared/ai-skills/sdp-workspace-setup/SKILL.md` (1 mention). Narrative docs: `sdp-shared/docs/SDP-Flowchart.md` (1 mention), `SDP-Project-Evolution.md` (2 — a `sdp-tone.ps1` invocation example and an LLM-Only table row), `SDP-Script-Authoring.md` (1), `SDP-Skill-Authoring.md` (1), `SDP-Standards-Setup.md` (1 — a `/sdp-read-docs` slash-command example), `SDP-Tone-Notifications.md` (1), `SDP-Workspace-Setup.md` (3), and `README.md` (3 — repo-tree comment, LLM-Only table row, skill-description table row). Scripts: `sdp-shared/scripts/sdp-create-prompt.ps1` (1 literal mention inside a generated-prompt narrative string) and `sdp-shared/scripts/sdp-report-log-loop-metrics.ps1` (1 literal mention inside a report-narrative here-string, alongside the already-renamed `sdp-project-pre-work-verify`/`sdp-project-run-prompt` from prior entries). Test file `sdp-shared/scripts/tests/sdp-preflight.Tests.ps1` (3 mentions — verified directly rather than assumed from the design doc's plan-time count: these are generic `hook-registered` validator fixture strings using the skill name as an arbitrary example command, not behavior keyed to this skill's actual identity, but renamed anyway for literal-string consistency). Bootstrap doc (`SDP_Sapient-Driven-Principles_v1.1.0.md`)'s seven literal mentions (the Workspace Setup section's Agent note, the Reference-documents note, the `sdp-docs/00_prompt.txt` Section 2 dispatch-file bullet, three duplicate already-superseded WORKER-session/REVIEWER-session/GATE_REVIEWER-session SKILLS CHECK blocks — each already carrying `sdp-pre-work-verify` and `sdp-run-prompt` Corrections from prior entries, a third, separate dated Correction appended to each for this skill — and the Project Context Document creation instruction) corrected via strikethrough + a new dated Correction appended immediately after each, per Append-Only Discipline — not rewritten in place. Also updated, as living tracking indexes rather than point-in-time snapshots (same treatment given to these same two files in all four prior entries): `~SDP-Maintenance/~docs/skill-catalogue.md` (catalogue row, LLM-Only list) and `~SDP-Maintenance/~docs/sdp-create-banner-adoption-tracker.md` (per-skill progress row, plus 3 narrative mentions in the Totals line and the Post-Run Gap Audit section describing this skill's own historical post-run gap fix — all still describing the same skill under its current name, not a frozen historical snapshot). Left unchanged, deliberately: `~SDP-Maintenance/~sdp-shared/~skill-evals/sdp-read-docs-eval.md`, `sdp-auto-eval.md`, `sdp-state-loop-start-eval.md`, and `~SDP-Maintenance/~sdp-maintenance-scripts/reports/skill-script-audit-report-20260722-1400.md` (point-in-time historical records, immutable snapshots of what was true when written — same convention already applied to every prior rename/relocation entry in this file); `.sdp-solution-workflow/logging/**/*.jsonl` and `sdp-solution-docs/log-reports/**/*.md` (raw historical event logs and generated reports); and `project-scope-skill-naming-design.md` itself (a planning record of this very rename). Also judged out of scope, on individual review of each file's own status header and the specific sentence containing the hit (not a generic same-class appeal): `~SDP-Maintenance/~docs/sdp-solution-setup-json-design.md` — status "Design ... no code or doc changes have been made from this file," dated 2026-07-18; its one mention is a design-time illustrative example of a proposed `hook-registered` manifest check (`commandContains: "sdp-read-docs"`), and the actual current `.claude/settings.local.json` contains no literal `sdp-read-docs` string at all — confirming this was never the implemented value, only a design-time example, so renaming it would misrepresent the historical proposal; and `~SDP-Maintenance/~docs/solution-coordinator-orchestration-design.md` — status "Design ... No skill file has been changed yet," dated 2026-07-19; its one mention is a point-in-time inventory of which project-level skills had a solution-level companion as of that date, used to justify a scope decision made then — the same file already excluded for the same reason during the `sdp-run-prompt` rename (entry above).
| 1.1.0 | 0 | 2026-07-23 | No version bump (skill rename, no behavior change). `sdp-loop-prep` renamed to `sdp-project-loop-prep` — fourth of 11 project-scoped skills renamed under the `sdp-project-*` prefix convention (mirrors the existing `sdp-solution-*` convention; see `~SDP-Maintenance/~docs/project-scope-skill-naming-design.md` §8 for the full plan, §9 for the execution log, and the prior three 2026-07-23 entries below documenting skills 1–3 of 11). Renamed via `git mv` to preserve history: `.claude/skills/`, `sdp-shared/ai-skills/`. Every literal-identifier reference updated to match: `SDP-Solution-Setup.json` (`skill-pair` entry), the two SKILL.md files' own self-references (L1 frontmatter `name`, both tone-invocation `-skillName` calls, the L1 self-referencing Read-path and halt-message path, the L2 Purpose paragraph ×2, Step 0 halt banner text, `loop_prep` state-field `purpose` string, and Constraints bullet), cross-references in `.claude/skills/sdp-phase-rightsizing-check/SKILL.md` and `.claude/skills/sdp-source-coverage-check/SKILL.md` (description field, 1 mention each), `sdp-shared/ai-skills/sdp-phase-rightsizing-check/SKILL.md` (4 mentions), `sdp-shared/ai-skills/sdp-source-coverage-check/SKILL.md` (2 mentions), and `sdp-shared/ai-skills/sdp-solution-loop-prep/SKILL.md` (8 mentions — its own self-reference to the sibling `sdp-solution-loop-prep` name confirmed untouched throughout). Narrative docs: `sdp-shared/docs/SDP-Flowchart.md` (2 mentions), `README.md` (skill table row, prose sentence, command-list line), `QuickStart.md` (1 mention). Bootstrap doc (`SDP_Sapient-Driven-Principles_v1.1.0.md`)'s one literal mention (the Phase 5/7 rightsizing-bias note's two adjacent `sdp-loop-prep` references) corrected via strikethrough + a single dated Correction appended immediately after, per Append-Only Discipline — not rewritten in place. Also updated, as living tracking indexes rather than point-in-time snapshots (same treatment given to these same two files in all three prior entries): `~SDP-Maintenance/~docs/skill-catalogue.md` (catalogue row, LLM-Only list) and `~SDP-Maintenance/~docs/sdp-create-banner-adoption-tracker.md` (per-skill progress row, and the `sdp-phase-rightsizing-check` Tier-3 row's cross-reference). Left unchanged, deliberately: `~SDP-Maintenance/~sdp-shared/~skill-evals/sdp-loop-prep-eval.md`, `sdp-phase-rightsizing-check-eval.md`, `sdp-source-coverage-check-eval.md`, and `~SDP-Maintenance/~sdp-maintenance-scripts/reports/skill-script-audit-report-20260722-1400.md` (point-in-time historical records, immutable snapshots of what was true when written — same convention already applied to every prior rename/relocation entry in this file); `.sdp-solution-workflow/logging/**/*.jsonl` (raw historical event logs); and `project-scope-skill-naming-design.md` itself (a planning record of this very rename). Also judged out of scope, following the same-class precedent already established for `solution-coordinator-orchestration-design.md`/`-plan.md` in the prior entry: `~SDP-Maintenance/~docs/phase-pipeline-expansion-design.md`, `phase-pipeline-expansion-exploration.md`, `phase-pipeline-expansion-plan.md`, `solution-coordinator-orchestration-design.md`, `solution-coordinator-orchestration-plan.md`, and `solution-phase-dispatch-gap-design.md` — all dated design/exploration/plan records of decisions already closed or superseded, not living reference docs, each containing only narrative-past-tense mentions of this skill's old name. No hit found, confirmed no-op: `.ps1` scripts (literal or constructed), `sdp-shared/scripts/tests/*.Tests.ps1`, `.claude/settings.local.json`, and `sdp-shared/scripts/script-support/SDP-Tones.json` — no exact-match reference to this skill in any of the four. |
| 1.1.0 | 0 | 2026-07-23 | No version bump (skill rename, no behavior change). `sdp-run-prompt` renamed to `sdp-project-run-prompt` — third of 11 project-scoped skills renamed under the `sdp-project-*` prefix convention (mirrors the existing `sdp-solution-*` convention; see `~SDP-Maintenance/~docs/project-scope-skill-naming-design.md` §8 for the full plan, §9 for the execution log, and the prior two 2026-07-23 entries below documenting skills 1 and 2 of 11). Renamed via `git mv` to preserve history: `.claude/skills/`, `sdp-shared/ai-skills/`. Every literal-identifier reference updated to match: `SDP-Solution-Setup.json` (`skill-pair` entry), the two SKILL.md files' own self-references (L1 frontmatter `name`, both tone-invocation `-skillName` calls, the L1 halt-message path string, the L2 "no further steps" closing-line self-reference), the backing script `sdp-shared/scripts/sdp-run-prompt.ps1`'s own `.SYNOPSIS` comment (the script file itself is NOT renamed — only its self-descriptive text), cross-references in `sdp-shared/ai-skills/sdp-evaluate-skill/SKILL.md` (argument example), `sdp-shared/ai-skills/sdp-auto/SKILL.md` (3 mentions), `sdp-shared/ai-skills/sdp-solution-state-loop/SKILL.md`, `sdp-shared/ai-skills/sdp-state-loop/SKILL.md` (4 mentions), `sdp-shared/ai-skills/sdp-state-loop-start/SKILL.md`, `README.md` (skill table ×2 — script-filename row `sdp-run-prompt.ps1` correctly left as-is), `SDP-Flowchart.md`, `SDP-Project-Evolution.md` (4 mentions — script-filename mentions within description text correctly left as-is), `SDP-Script-Authoring.md`, `SDP-Workspace-Setup.md` (Quick Start command list, skill-pair enumeration list), `sdp-shared/scripts/sdp-report-log-loop-metrics.ps1` (2 literal mentions inside code comments/report-narrative string — script-filename mentions elsewhere in the same file correctly left as-is). Also updated, as living tracking indexes rather than point-in-time snapshots (same treatment given to these same two files in both prior entries): `~SDP-Maintenance/~docs/skill-catalogue.md` (catalogue row, Hybrid — Applied list) and `~SDP-Maintenance/~docs/sdp-create-banner-adoption-tracker.md` (per-skill progress row). Bootstrap doc (`SDP_Sapient-Driven-Principles_v1.1.0.md`)'s seven literal mentions (the `sdp-docs/00_prompt.txt` section's invoke instruction; the three already-superseded WORKER-session/REVIEWER-session/GATE_REVIEWER-session SKILLS CHECK blocks, each already carrying a `sdp-pre-work-verify` Correction from the prior entry — a second, separate dated Correction appended to each for this skill; the WORKER- and REVIEWER-dispatch "Notify user" messages; and the Loop-orchestrated dispatch path bullet) corrected via strikethrough + a new dated Correction appended immediately after each, per Append-Only Discipline — not rewritten in place. Left unchanged, deliberately: `~SDP-Maintenance/~sdp-shared/~skill-evals/sdp-run-prompt-eval.md`, `sdp-state-loop-eval.md`, `sdp-worker-eval.md`, and `~SDP-Maintenance/~sdp-maintenance-scripts/reports/skill-script-audit-report-20260722-1400.md` (point-in-time historical records, immutable snapshots of what was true when written — same convention already applied to every prior rename/relocation entry in this file); `~SDP-Maintenance/~docs/solution-coordinator-orchestration-plan.md` and `~SDP-Maintenance/~docs/solution-coordinator-orchestration-design.md` (dated design/rationale records of a past decision, not living reference docs); `.sdp-solution-workflow/logging/**/*.jsonl` and `sdp-solution-docs/log-reports/**/*.md` (raw historical event logs/reports); and `project-scope-skill-naming-design.md` itself (a planning record of this very rename). No hit found, confirmed no-op: `sdp-shared/scripts/tests/*.Tests.ps1` (the design doc's plan-time count of 2 hits for this skill did not reproduce — a fresh grep found zero, noted as a discrepancy against stale plan-time data, not a miss), `.claude/settings.local.json` (its one hit is the unrelated `sdp-run-prompt.ps1` script-path permission entry), and `sdp-shared/scripts/script-support/SDP-Tones.json` (its one hit is the unrelated sibling `sdp-solution-run-prompt` tone-assignment entry) — no exact-match reference to this skill's own name in any of the three. |
| 1.1.0 | 0 | 2026-07-23 | No version bump (skill rename, no behavior change). `sdp-doc-review` renamed to `sdp-project-doc-review` — second of 11 project-scoped skills renamed under the `sdp-project-*` prefix convention (mirrors the existing `sdp-solution-*` convention; see `~SDP-Maintenance/~docs/project-scope-skill-naming-design.md` §8 for the full plan, §9 for the execution log, and the prior 2026-07-23 entry below documenting skill 1 of 11). Renamed via `git mv` to preserve history: `.claude/skills/`, `sdp-shared/ai-skills/`. Every literal-identifier reference updated to match: `SDP-Solution-Setup.json` (`skill-pair` entry), the two SKILL.md files' own self-references (L1 frontmatter `name`, both tone-invocation `-skillName` calls, the L2 `purpose` field's two mentions), cross-references in `sdp-shared/ai-skills/sdp-coordinator/SKILL.md` (2 mentions), `sdp-shared/ai-skills/sdp-loop-prep/SKILL.md` (4 mentions) and `.claude/skills/sdp-loop-prep/SKILL.md` (description), `sdp-shared/ai-skills/sdp-new-concept-intake/SKILL.md`, `sdp-shared/ai-skills/sdp-phase-rightsizing-check/SKILL.md` (6 mentions), `sdp-shared/ai-skills/sdp-solution-loop-prep/SKILL.md` and `.claude/skills/sdp-solution-loop-prep/SKILL.md` (description), `sdp-shared/ai-skills/sdp-source-coverage-check/SKILL.md` (3 mentions), `README.md` (skill table, `sdp-loop-prep` row), `QuickStart.md` (Automated Gate Check bullet), `SDP-Flowchart.md`, `SDP-Tone-Notifications.md` (`milestone.doc_certified` row), `SDP-Workspace-Setup.md` (skill-pair enumeration list). Also updated, as living tracking indexes rather than point-in-time snapshots (same treatment given to these same two files in the prior entry): `~SDP-Maintenance/~docs/skill-catalogue.md` (catalogue row, LLM-Only list) and `~SDP-Maintenance/~docs/sdp-create-banner-adoption-tracker.md` (excluded-skills list, per-skill Tier-4 row). Bootstrap doc (`SDP_Sapient-Driven-Principles_v1.1.0.md`)'s one literal mention (Implementation Loop step 1b, DOC REVIEW CHECK) corrected via strikethrough + a new dated Correction appended immediately after, per Append-Only Discipline — not rewritten in place. Left unchanged, deliberately: `~SDP-Maintenance/~sdp-shared/~skill-evals/sdp-doc-review-eval.md`, `~SDP-Maintenance/~sdp-shared/~skill-evals/sdp-coordinator-eval.md`, `sdp-loop-prep-eval.md`, `sdp-phase-rightsizing-check-eval.md`, `sdp-source-coverage-check-eval.md`, and `~SDP-Maintenance/~sdp-maintenance-scripts/reports/skill-script-audit-report-20260722-1400.md` (point-in-time historical records, immutable snapshots of what was true when written — same convention already applied to every prior rename/relocation entry in this file); `~SDP-Maintenance/~docs/solution-coordinator-orchestration-plan.md` (a dated design/rationale record of a past decision, not living reference docs); `.sdp-solution-workflow/logging/**/*.jsonl` (raw historical event logs); and `project-scope-skill-naming-design.md` itself (a planning record of this very rename). No hit found, confirmed no-op: `.ps1` scripts (literal or constructed), `sdp-shared/scripts/tests/*.Tests.ps1`, `.claude/settings.local.json`, and `sdp-shared/scripts/script-support/SDP-Tones.json` — no exact-match reference to this skill in any of the four. |
| 1.1.0 | 0 | 2026-07-23 | No version bump (skill rename, no behavior change). `sdp-pre-work-verify` renamed to `sdp-project-pre-work-verify` — first of 11 project-scoped skills renamed under the `sdp-project-*` prefix convention (mirrors the existing `sdp-solution-*` convention; see `~SDP-Maintenance/~docs/project-scope-skill-naming-design.md` §8 for the full plan and remaining rename order). Renamed via `git mv` to preserve history: `.claude/skills/`, `sdp-shared/ai-skills/`. Every literal-identifier reference updated to match: `SDP-Solution-Setup.json` (`skill-pair` entry), the two SKILL.md files' own self-references (L1 frontmatter `name`, both tone-invocation `-skillName` calls, the L2 read-path and halt-message path references — the L2 file itself has no self-reference to its own name), cross-references in `sdp-shared/ai-skills/sdp-doc-review/SKILL.md`, `sdp-shared/ai-skills/sdp-solution-phase-worker/SKILL.md` (2 mentions), and `sdp-shared/ai-skills/sdp-worker/SKILL.md`, `README.md` (skill table), `SDP-Project-Evolution.md` (LLM-required table), `SDP-Workspace-Setup.md` (skill-pair enumeration list, Skill Authoring Gaps worked-example pointer), `SDP-Skill-Authoring.md` (Worked Example section retitled, both its Level 1/Level 2 code blocks updated throughout), `sdp-report-log-loop-metrics.ps1` (literal mention inside the idle/overhead-components report narrative string). Also updated, as living tracking indexes rather than point-in-time snapshots (same treatment the `sdp-loop-metrics-report` → `sdp-report-log-loop-metrics` rename, 2026-07-17 entry below, gave these same two files): `~SDP-Maintenance/~docs/skill-catalogue.md` (catalogue row, LLM-Only list) and `~SDP-Maintenance/~docs/sdp-create-banner-adoption-tracker.md` (per-skill progress row). Also updated: `~SDP-Maintenance/~docs/Towards self-driving codebases/self-driving-codebases-04-sdp-recommendations.md` (LH-1 recommendation's worked-example pointer — cited elsewhere as still-tracked authoring guidance, not a closed historical snapshot). Bootstrap doc (`SDP_Sapient-Driven-Principles_v1.1.0.md`)'s four literal mentions (the WORKER-session, REVIEWER-session, and GATE_REVIEWER-session already-superseded SKILLS CHECK blocks, plus the Dispatch and Halt Contracts skill-authoring-reference blockquote) corrected via strikethrough + a new dated Correction appended immediately after each, per Append-Only Discipline — not rewritten in place. Left unchanged, deliberately: `~SDP-Maintenance/~sdp-shared/~skill-evals/sdp-pre-work-verify-eval.md` and `sdp-worker-eval.md`, `~SDP-Maintenance/~sdp-maintenance-scripts/reports/skill-script-audit-report-20260722-1400.md` (point-in-time historical records, immutable snapshots of what was true when written — same convention already applied to every prior rename/relocation entry in this file), and `project-scope-skill-naming-design.md` itself (a planning record of this very rename). Confirmed no-op: `.claude/settings.local.json`, `sdp-shared/scripts/script-support/SDP-Tones.json`, and `sdp-shared/scripts/tests/*.Tests.ps1` — no exact-match reference to this skill found in any of the three. |
| 1.1.0 | 0 | 2026-07-21 | No version bump (feature addition — Phase 1/3 interactive brainstorm capture, closing a dogfooding gap). Per `~SDP-Maintenance/~docs/done/phase-1-3-interactive-capture-design.md`: a user reached Phase 3 (Expanded Concept) expecting an interactive session to incorporate Phase 2 research into the Phase 1 concept — instead `sdp-solution-phase-coordinator` fired a non-interactive `sdp-solution-phase-worker` subagent, same as any other phase task. Three sources disagreed on the intended behavior: the bootstrap doc's "Brainstorming capture rule" never stated which role runs it; the Superpowers Integration Policy table already correctly scoped `/brainstorming` to COORDINATOR-only (WORKER/REVIEWER both "Role mismatch"); and `README.md` falsely claimed WORKER "invokes `/brainstorming` automatically" — grepped, never implemented anywhere. Separately, Phase 3 had no "Mechanics" section at all (unlike Phase 2/7), so nothing told a WORKER drafting it to actually incorporate Phase 2's findings. Implemented in `7b08239` (2026-07-21 20:09): `sdp-solution-phase-coordinator` now runs `/brainstorming` interactively with the user before dispatching a WORKER task for Phase 1 or Phase 3, transcribing every decision into the phase document (append-only) before the COORDINATOR session closes — WORKER is then dispatched to formalize from that captured material, never originating content from nothing, preserving Role Separation; `sdp-solution-phase-worker` now reads `01_concept.md` + `02_research_findings.md` for Phase 3 and treats any captured brainstorm material as its starting point; bootstrap doc gained a new "Phase 1 / Phase 3 — Interactive Capture Mechanics" section plus a first-ever Phase 3 Mechanics + Gate entry, and corrected the stale `02_expanded_concept.md` filename reference (superseded by the 2026-07-18 phase renumbering) via strikethrough + dated correction; `README.md`'s false automatic-WORKER-brainstorm claim corrected to COORDINATOR. **Same-day fix folded into the same commit** (per the design doc's own "Follow-up" note): the initial pass brought `01_concept.md`/`02_research_findings.md` into the Phase 3 brainstorming session but not the original tracked source document from `sdp-solution-docs/user-design-docs/processed/` — since Phase 1 is a deliberate compression of that source, drafting Phase 3 from the compression alone reintroduced the same loss-of-detail risk the interactive capture was meant to reduce; both `sdp-solution-phase-coordinator` (before opening the brainstorming session, Phase 1 and Phase 3 alike) and `sdp-solution-phase-worker` (Phase 3's Step 3 read) now also read the tracked source document when one exists for the cycle, additive to — not a replacement for — `sdp-solution-source-coverage-check`'s mandatory backstop. Confirmed present in the current bootstrap doc (verified directly in this same review session, matching the design's description in full). Same commit also added `sdp-shared/docs/SDP-Flowchart.md` (a new stage-by-stage project-lifecycle walkthrough) and a naming-consistency cleanup for `sdp-solution-coordinator` → `sdp-solution-phase-coordinator` references left over from the prior day's dispatch-gap fix. **Entry added retroactively** during a 2026-08-13 docs-review pass — verified against live repo content and git history at review time, not from a contemporaneous implementation-session transcript.
| 1.1.0 | 0 | 2026-07-21 | No version bump (feature addition — closes the solution-scoped phases 1-7 WORKER/REVIEWER/GATE_REVIEWER dispatch gap). Per `~SDP-Maintenance/~docs/done/solution-phase-dispatch-gap-design.md`: a real-world session dispatching a Phase 1 REVIEWER pass hit a hard error — `sdp-solution-coordinator`'s Step 2a said to dispatch phases-1-7 WORKER/REVIEWER/GATE_REVIEWER via `sdp-solution-create-prompt`, but that skill only ever accepted `role ∈ {SOLUTION_COORDINATOR, SOLUTION_WORKER, SOLUTION_REVIEWER}` and hard-errored on a null `active_solution_task` (always null during phases 1-7, which use the sibling `current_phase` field instead); `sdp-solution-run-prompt`'s routing table didn't recognize the other roles either; and `sdp-worker`/`sdp-reviewer` both halt outright on a dispatch with no `sdp-project_*` path segment — exactly what a solution-scoped dispatch looks like. Root cause: `solution-coordinator-orchestration-design.md`'s own implementation-impact inventory never mentioned `sdp-solution-create-prompt` at all. **Resolved with two new dedicated skills rather than modifying proven, heavily-used project-level code** (explicit user principle: project-level skills are never modified for a solution-level need) — `sdp-worker`/`sdp-reviewer` receive zero changes. Implemented in `d32bc1a` (17:54) + paired docs commit `e595ed1` (17:54): new `sdp-solution-phase-worker`/`sdp-solution-phase-reviewer` (solution-scoped WORKER/REVIEWER, referencing the bootstrap doc's TDD/debugging/Pre-Work-Verification protocols rather than restating them); new `sdp-solution-gate-review` as GATE_REVIEWER companion to `sdp-gate-review`, which had its `-scope project|solution` parameter (added by the original orchestration design) removed entirely and reverted to project-only, closing the risk of an agent pointing the project-level skill at solution work — also reverting Phase-Readiness gate handling to its original nine-criteria project-only form, since the ledger's "dependency edge validity" tenth criterion is solution-only; new `sdp-solution-phase-coordinator`, a straight copy of `sdp-solution-coordinator` (skill + backend script) with two targeted fixes — graceful `"no_shared_task"` status instead of hard-erroring on null `active_solution_task`, and writing the session dispatch file directly instead of delegating to `sdp-solution-create-prompt` (a real-world session had also found Step 2a unreachable in every case, since the parent skill's unconditional script invocation hard-errored before Step 2a's dispatch logic ever ran) — `sdp-solution-coordinator` itself received exactly one documentation-only pointer line at Step 2a, nothing else; `sdp-solution-create-prompt`/`sdp-solution-run-prompt` gained the phases-1-7 branch/routing; `sdp-initialize-sdp`'s banner and the bootstrap doc corrected to match; README/QuickStart corrected for the new split. **Same-day follow-up, `057e83b` (18:33):** renamed `sdp-solution-gate-review` → `sdp-solution-phase-gate-review` for consistency with its three siblings (an accidental naming omission, not a deliberate distinction — it has no post-Phase-7 role, same as the other three), applied as a straight rename across its own files and every cross-reference; also fixed a `$lastSession` zero-padding bug in its `-setup.ps1`, found during the rename and — on inspection — confirmed as an identical pre-existing bug in the original `sdp-gate-review-setup.ps1`, fixed there too rather than left as a known duplicate. Tier 3 housekeeping (README/skill-catalogue tracking rows) — explicitly left outstanding by the design doc at the time — confirmed present in the current repo (`README.md`, `~SDP-Maintenance/~docs/skill-catalogue.md` both carry the four new skill names), picked up in a later sync pass rather than a dedicated commit. **Entry added retroactively** during a 2026-08-13 docs-review pass — verified against live repo content and git history at review time, not from a contemporaneous implementation-session transcript.
| 1.1.0 | 0 | 2026-07-20 | No version bump (skill-guidance addition, no new capability). Per `~SDP-Maintenance/~docs/done/concept-intake-brainstorm-path-design.md`: conversational-mode concept cycles never produced a tracked source document, so they never got `sdp-source-coverage-check`'s safety net, and neither `sdp-workspace-setup`'s post-setup "Next Step" banner nor the bootstrap doc's Phase 1 intake-ownership guidance told a user without an existing doc that running `/brainstorming` first (saving its output to `sdp-solution-docs/user-design-docs/` instead of Superpowers' own default spec location) was an option that would get them both `/brainstorming`'s full rigor and the coverage-check safety net. Implemented via `26b571d` (2026-07-20 23:23): `sdp-workspace-setup/SKILL.md` Step 3's banner split into `Next Step — Have Docs` / `Next Step — No Docs Yet` rows (both the base first-run banner and the "new project" pause hand-off variant); the bootstrap doc's Phase 1 — Source-Doc Intake Ownership "if no" branch struck through and replaced with a matching two-way split (documented as its own `Correction — 2026-07-20` blockquote, per Append-Only Discipline). `sdp-new-concept-intake` itself received no changes — its existing document-driven mode already picks up a brainstormed doc exactly like a hand-dropped one; the gap was entirely in upstream guidance, not downstream capability. Conversational mode is kept, unchanged, as a third option for concepts that don't warrant full brainstorming rigor. **Entry added retroactively** during a 2026-08-13 docs-review pass — verified against live repo content and git history at review time, not from a contemporaneous implementation-session transcript.
| 1.1.0 | 0 | 2026-07-20 | No version bump (feature addition — phases 1-7 became solution-scoped instead of project-scoped, plus a new cross-project dependency ledger). Per `~SDP-Maintenance/~docs/done/solution-coordinator-orchestration-design.md` (dated 2026-07-19, closing an open question about `last_active_projects` being misleadingly arbitrary): phases 1-7 (Concept through Phase Readiness) now run once per solution rather than once per project — project identity first appears at Phase 7's decomposition. Implemented across five same-day commits: `5651b0d` (10:46) added the `.sdp-solution-workflow/state.json` template — `current_phase`/`phase_gate`/`phase_readiness`/`gpg_version`, identical shape to the project-level template, as siblings to the existing shared-task fields; `8333f2e` (11:49) gave `sdp-solution-coordinator` the phases-1-7 driving role and Phase 7 dependency-declaration support; `2c92200` (11:54) added the dependency ledger's RED/YELLOW/BLUE/GREEN light-resolution state machine (`.sdp-solution-workflow/dependencies.md`/`dependencies.json`, `light_attempts` stuck-dependency counter, mirroring the existing `eval_cycle_attempts` escalation pattern); `4eb7453` (11:59) added post-Phase-7 dispatch gating (a project's coordinator is only invoked when every dependency edge where it's the consumer is GREEN), the solution-level Phase Readiness backward-regression procedure, and migration detection for already-in-flight solutions; `1f265bf` (12:23) registered the new `sdp-solution-state-loop`/`sdp-solution-loop-prep` skills and the ledger files in the preflight manifest. A same-day post-hoc fix, `1ddc646` (15:53), closed three gaps a whole-branch review found after all 15 plan tasks individually passed: `SDP-Workspace-Setup.md`'s solution-root setup checklist never actually created the two ledger files despite the manifest registering them as required (every `sdp-solution-coordinator` invocation for a solution still in phases 1-6 would fail preflight and halt) — added stub creation; `sdp-solution-coordinator/SKILL.md` had narrowed `-scope solution` to Phase-Readiness-only gate dispatches when the design calls for it on all seven phases — broadened; `sdp-gate-review/SKILL.md` documented `-scope solution` in prose but never operationalized it in the Procedure — added a scope-determining Step 0 and threaded the parameter through. **Boundary invariant preserved as designed:** project-level `sdp-coordinator`/`sdp-worker`/`sdp-reviewer` received zero changes — the ledger is read/written exclusively by `sdp-solution-coordinator`; a project only ever receives an ordinary acceptance criterion or an ordinary new registry task, never a ledger-aware concept. **Superseded in part the next day:** `solution-phase-dispatch-gap-design.md` (2026-07-21, separately logged) closed a same-day-discovered gap in phases-1-7 WORKER/REVIEWER/GATE_REVIEWER dispatch by introducing dedicated `sdp-solution-phase-coordinator`/`-worker`/`-reviewer`/`-gate-review` skills — `sdp-solution-coordinator/SKILL.md` still carries this design's Step 2 phases-1-7 mechanics text, but its own in-file commentary now marks large portions (e.g. Step 2d) as dead code no longer actually invoked, since the newer sibling skill drives that role today; the ledger/dispatch-gating/regression mechanics this entry documents remain live and unaffected. See the design doc for full rationale (four internal gap-review rounds plus a consistency pass) and `~SDP-Maintenance/~docs/done/solution-coordinator-orchestration-plan.md` for the 15-task implementation record. **Entry added retroactively** during a 2026-08-13 docs-review pass — verified against live repo content and git history at review time, not from a contemporaneous implementation-session transcript.
| 1.1.0 | 0 | 2026-07-18 | Phase pipeline expanded 5→7: new Phase 2 (Research, internal WORKER fan-out, one child subagent per research angle) and new Phase 7 (Phase Readiness — build-phase decomposition moved from old Phase 5, plus a full-lifecycle source-doc traceability audit with GATE_REVIEWER-proposed remediation and COORDINATOR-executed backward `current_phase` regression — SDP's first backward phase transition). Old Phase 2→3 (Expanded Concept), Phase 3→4 (Architecture), Phase 4→5 (Implementation Overview), Phase 5→6 (Refined Implementation Plan, decomposition responsibility removed). `state.json` schema gains `phase_readiness` (regression record) and each phase state file gains `gpg_chapters`. See `~SDP-Maintenance/~docs/phase-pipeline-expansion-design.md` for full rationale, and `phase-pipeline-expansion-plan.md` for the task-by-task implementation record. |
| 1.0.0 | 0 | 2026-07-18 | No version bump (feature addition — solution-level preflight manifest, closing a real bug surfaced during a consumer project's multi-project setup session, not discovered internally). A genuinely multi-project SDP consumer workspace hit a real failure: `sdp-preflight.ps1`'s `skill-pair` check type hardcodes `.claude/skills/<name>/SKILL.md` and `sdp-shared/ai-skills/<name>/SKILL.md` relative to `-workspaceRoot`, with no override field — always fails when `-workspaceRoot` is a per-project folder (the documented invocation for every dispatch site), since `.claude/`/`sdp-shared/`/`standards/` only exist at the solution root, not inside `sdp-project_*/`. The same applied to the GPG-version `json-value` check and solution-root script `file-exists` checks. This matched an already-scoped, not-yet-implemented design in this repo (`~SDP-Maintenance/~docs/sdp-solution-setup-json-design.md`, authored earlier the same day) — implemented here. **Script (`sdp-shared/scripts/sdp-preflight.ps1`):** three new check types — `hook-registered` (substring-match a `commandContains` string against any hook step's `.command`/`.args` under `.hooks.<event>` in a settings JSON file), `json-array-contains` (array-membership check via the existing dotted-pointer walker), `glob-exists` (directory + filename-glob existence). New `Resolve-ManifestFilename` function auto-detects which manifest to load from `-workspaceRoot` alone (no new parameter, zero call-site changes needed for existing usage): a `sdp-project_*` path segment → `SDP-Workspace-Setup.json` (unconditionally, even if a stray `SDP-Solution.json` also sits there); else `SDP-Solution.json` present with `last_active_projects` other than the legacy `["."]` collapse → `SDP-Solution-Setup.json`; `last_active_projects == ["."]` → still `SDP-Workspace-Setup.json` (matches the bootstrap doc's own Project Resolution Order Level 3 collapse); neither marker present → operational error (`error` names the condition, `manifestUsed: null`), not a silent guess. New `manifestUsed` field added to every envelope shape (success, `-WhatIf`, and every `New-OperationalError` call site). **Design self-inconsistency caught and corrected during implementation, not shipped as spec'd:** the design's own §2e claimed existing flat-root Pester usage would be "unaffected," which is false as the function is written — a bare temp-dir fixture with neither marker present resolves to `$null` (operational error) under the new logic, and no existing test ever wrote `SDP-Solution.json`. Fixed by making `New-TestWorkspace` also plant a minimal legacy-collapse `SDP-Solution.json` (`last_active_projects: ["."]`) by default — every pre-existing test keeps resolving to `SDP-Workspace-Setup.json` exactly as before; the true "neither marker" case is now a separately, explicitly tested edge case rather than the accidental default. New `Context "Manifest scope resolution"` (6 cases: project-segment precedence over a stray non-collapse solution marker, genuine solution-scope resolution, explicit legacy-collapse case, neither-marker operational error, missing-solution-manifest error naming the right filename, unparseable solution-marker error) and `Context "New validator types"` (6 cases, pass/fail pairs for all three new check types) added to `sdp-shared/scripts/tests/sdp-preflight.Tests.ps1`. Full suite verified: 35/35 passing, zero regressions. **Manifest data:** authored `SDP-Solution-Setup.json` fresh at the solution root — skill-pair entries for all 32 current two-file skills (enumerated directly from `.claude/skills/*/SKILL.md` vs `sdp-shared/ai-skills/*/SKILL.md` on disk, not trusted from the stale fossil or the design doc's own table, both confirmed already out of date against skills shipped after 2026-06-21), `file-exists` for the 3 Level-1-only skills' scripts/configs (`sdp-tone`, `sdp-new-claude-terminal`, `sdp-create-banner`) and the 9 other solution-root scripts (`sdp-github.ps1`, the 5 `sdp-report-log-*`/`sdp-report-logs-combine` scripts, `sdp-hook-log.ps1` + its tool-gating config, `sdp-workflow-log.ps1`), one `hook-registered` check on the `SessionStart` hook, 20 `json-array-contains` checks (one per `sdp-` script's `permissions.allow` entry in `.claude/settings.local.json`), and `dir-exists`/`glob-exists` for the GPG standards folder/doc. Per the design's §2c recommendation ("author fresh, delete outright" — its stale skill-pair list and the new check types it lacked made in-place editing no cheaper than a rewrite), deleted the stale pre-split `SDP-Workspace-Setup.json` fossil that had sat at the solution root since 2026-06-21 (three days before the `sdp-project_*` multi-project convention existed, per the 2026-06-24 entry below) and was never actually read by any real invocation (nothing invokes preflight at solution-root scope until this same pass — see next item). Verified end-to-end with a real, non-`-WhatIf` run against this repo's own root: `manifestUsed: "SDP-Solution-Setup.json"`, `ok: true`, 0 failures across all 74 checks. **Residual, deliberately unfixed here:** this repo's own `SDP-Solution.json` has `last_active_projects: []` (empty), not the legacy `["."]` — an artifact of this repo being the framework's own dev workspace (`~SDP-Maintenance` mode) rather than a workspace that ever completed a real single- or multi-project Solution Root Setup — so it now correctly resolves to `SDP-Solution-Setup.json` rather than erroring; noted for the record, not a defect. **Docs and dispatch wiring:** `SDP_Sapient-Driven-Principles_v1.0.0.md` — (a) applied the Phase Gate `-workspaceRoot` correctness fix from design §2b via strikethrough + a dated Correction blockquote, per Append-Only Discipline: the Phase Gate Procedure's PREFLIGHT CHECK step showed the `sdp-preflight.ps1` invocation as inline prose with no `-workspaceRoot` argument at all, unlike this doc's other two invocations — a real, independent bug (a gate review run from a multi-project solution would have silently validated the wrong project's manifest), now consistent; (b) added a new "Solution Dispatch Loop" section adjacent to "Implementation Loop (Per Task)," documenting `sdp-solution-coordinator`'s fan-out/cycle-sync procedure including a step-0 PREFLIGHT CHECK. `sdp-shared/ai-skills/sdp-solution-coordinator/SKILL.md` — added that step-0 PREFLIGHT CHECK for real (`-workspaceRoot .`, branches on `ok`, applies the Halt Behavior Contract via `sdp-create-banner`): confirmed during design review this skill had zero `preflight` mentions before now. `sdp-shared/docs/SDP-Workspace-Setup.md` — registered `SDP-Solution-Setup.json` in the Solution Root Folder Structure diagram; flipped Solution Root Setup Step 4 ("Verify solution infrastructure") from hand-checked prose to `sdp-preflight.ps1 -workspaceRoot .`-driven verification, naming which sub-items stay manual (Superpowers plugin, PowerShell/Pester availability, standards-doc *version* confirmation with the user — a filesystem check can prove existence, not that it's the version the user intends); added the matching "Create `SDP-Solution-Setup.json` manifest" checklist item. **Self-contradiction fixed, found during this same edit:** the existing per-project "Create `SDP-Workspace-Setup.json` manifest" checklist item instructed authors to enumerate skill-pair/`sdp-tone`/script/GPG-version checks in the very same breath as a Note saying not to include `sdp-shared/`/`standards/`-rooted checks — those are exactly the same paths. Removed the enumeration from the per-project item now that those checks have an actual home; the Note is now consistent rather than self-contradicted by the sentence before it. **Separately, a stale-hook-name bug fixed because it was directly load-bearing for the new `hook-registered` check just added:** several mentions in this same doc (the session-start-hook explanation, a "To check" instruction, an example hook JSON `command` string, and a Step 4 checklist bullet) still named `sdp-read-docs`/`sdp-solution-read-docs` as the `SessionStart` hook's target; confirmed stale against both `sdp-shared/ai-skills/sdp-initialize-sdp/SKILL.md`'s own Purpose section ("Replaces `sdp-solution-read-docs` as the SessionStart hook's direct target") and this repo's actual live `.claude/settings.local.json`, whose real hook already invokes `sdp-initialize-sdp` — corrected all mentions to match what's actually shipped, since a future setup session copying the stale example JSON would write a hook command the new preflight check couldn't match. `sdp-shared/docs/SDP-Script-Authoring.md` — flipped its "Current state" deferred-item blockquote (previously: solution-root script existence "verified manually... until [`SDP-Solution-Setup.json`] exists") to describe the now-shipped feature and `Resolve-ManifestFilename`'s automatic scope detection; added a "When adding a new solution-root script" subsection mirroring the existing project-scoped one. **Explicitly out of scope for this pass, noted for a future one:** several backing scripts behind two-file skills (`sdp-create-prompt.ps1`, `sdp-run-prompt.ps1`, `sdp-solution-coordinator.ps1`, `sdp-solution-reviewer.ps1`, `sdp-solution-create-prompt.ps1`, the three `sdp-gate-review-*.ps1` helpers) have no existence check in either manifest — `skill-pair` only checks the two `SKILL.md` files, and none of these were named in the design's literal Step-4-derived inventory. Implemented via two independent, fully-scoped parallel workstreams (script+tests; docs+dispatch wiring) plus this manifest-authoring and final verification pass done directly — not through the formal WORKER/REVIEWER role machinery, since this repo has no phase/registry/state-file infrastructure for its own framework-maintenance work (confirmed: no `registry.md` or `state.json` exists anywhere under `~SDP-Maintenance/`). |
| 1.0.0 | 0 | 2026-07-18 | No version bump (user-directed folder relocation, no behavior change). `script-support/` (holding `SDP-Tones.json`, `sdp-create-banner-icons.json`, `sdp-hook-log-tools.json`, `SDP-Terminal-Sessions.json`) moved via `git mv` from `.sdp-solution-workflow/script-support/` to `sdp-shared/scripts/script-support/` — now a sibling folder to the scripts that read it, rather than a solution-workflow runtime folder. Updated to resolve the new sibling-relative location directly off `$PSScriptRoot` instead of via a computed `$workspaceRoot`: `sdp-tone.ps1` (`$configPath`; its `.NOTES` reads/writes comment), `sdp-hook-log.ps1` (`$configPath`; `.NOTES`), `sdp-create-banner.ps1` (`$registryPath`; dropped its now-unused `$workspaceRoot` local), `sdp-new-claude-terminal.ps1` (`$registryPath`; `.NOTES` Reads/Writes block — `$solutionRoot` retained, still used elsewhere in the script for `SDP-Config.json`). Updated path references (literal string only, no behavior change): `SDP-Workspace-Setup.json` (two `file-exists` checks), `.claude/skills/sdp-create-banner/SKILL.md` (Icon Registry section, Constraints bullet), `.claude/skills/sdp-tone/SKILL.md` (Reference section), `README.md` (tree — moved the `script-support/` block from under `.sdp-solution-workflow/` to under `sdp-shared/scripts/`), `QuickStart.md` (Automated Gate Check bullet), `SDP-Tone-Notifications.md` (header table, Overview), `SDP-Script-Authoring.md` (Workspace Root Resolution's "Solution-root scripts" paragraph reworded — `script-support/` is resolved as a sibling folder off `$PSScriptRoot`, not a `$workspaceRoot` subfolder; Worked Examples annotated-patterns table row for `sdp-create-banner.ps1`'s icon registry load), `SDP-Workspace-Setup.md` (folder diagram — moved the block from under `.sdp-solution-workflow/` to under `sdp-shared/scripts/`; Step 1 copy note — since `sdp-shared/` is copied wholesale, `script-support/` and its two framework-reference files now arrive with that copy rather than being created fresh in Step 2, which had its own bullet trimmed accordingly; SKILLS-CHECK-equivalent verification paragraph; "Create `SDP-Tones.json`"/"Create/Copy `sdp-hook-log-tools.json`" checklist items reworded to account for Step 1 possibly already having brought a copy along; per-project preflight-manifest guidance; `SDP-Tones.json` Template section). Bootstrap doc (`SDP_Sapient-Driven-Principles_v1.0.0.md`) had one literal-path mention, inside the 2026-07-17 hook-based-logging Addition blockquote — corrected via strikethrough + a new dated Correction blockquote appended immediately after, per Append-Only Discipline, not rewritten in place. Left unchanged, deliberately: historical `SDP-Changelog.md` entries mentioning the old path (immutable per-entry snapshots of what was true when written, same convention already applied to every prior rename/relocation entry in this file). |
| 1.0.0 | 0 | 2026-07-18 | No version bump (documentation-only — user-requested audit of the full report family: all 5 skills, their scripts, the logging/report folders, and every doc that references them; findings applied same session). Cross-cutting: all three raw-log report skills' scripts (`sdp-report-log-loop-metrics.ps1`/`-hook-metrics.ps1`/`-workflow-metrics.ps1`) emit an ASCII `->` placeholder in `period.titleDate` for multi-day periods with a script comment stating "the calling skill substitutes the proper glyph" — none of the three L2 SKILL.md files actually contained that instruction; added to each. `sdp-report-log-loop-metrics/SKILL.md`: added the undocumented `-OffHoursThresholdMinutes` script param to Inputs/Step 2; added the missing column template for the `offHoursWindows` table (its three sibling tables already had one); added the script's `pairingWarnings` field to the data-quality notes step (computed but never read by the skill). `sdp-report-log-hook-metrics/SKILL.md` and `sdp-report-log-workflow-metrics/SKILL.md`: cited each script's computed-but-unreferenced `header.distinctWorkItemNames`/`distinctPhaseNames` fields in their Summary steps. `sdp-report-log-combined-metrics`: L1 frontmatter description still said "full chronological event table," stale since the 2026-07-17 three-timeline redesign (L2 was already correct) — updated to match; `sdp-report-log-combined-metrics.ps1`'s `.NOTES` and generated Methodology text overclaimed that `detail` is surfaced in the Concerning Events table (only `reason` is, verified against `New-EventRow`) — corrected both. `SDP-Workspace-Setup.md`: folder diagram had zero representation of `sdp-solution-docs/log-reports/` (all four report-output subfolders, real and populated on disk) — added. `~SDP-Maintenance/~docs/skill-catalogue.md`: top-of-file "Catalogue now covers all 31 L1 skills" line was stale against its own bottom-line "Total: 35 skills" — reworded to scope the "31" figure to the historical 2026-07-17 refresh event rather than reading as a current total. `README.md`: the 5 report-family skills appeared only in the PowerShell Scripts table, not as skills — added a new "Reporting" subsection under Skills Inventory. `SDP-Script-Authoring.md`: all 5 report-family Worked Examples' permission-entry JSON snippets used a stale double-backslash path form (`.\\sdp-shared\\scripts\\...`) while the actual `.claude/settings.local.json` uses forward slashes (`./sdp-shared/scripts/...`, confirmed already-correct on `sdp-hook-log.ps1`'s own worked example) — corrected all 5; the same stale form exists elsewhere in this doc for non-report scripts, left out of scope for this pass. Confirmed clean, no changes needed: `SDP-Workspace-Setup.json` (report family intentionally absent from the preflight manifest — manual-invocation skills, not part of the dispatch pipeline), `.claude/settings.local.json` permission names, `SDP-Skill-Authoring.md`, `SDP-Tone-Notifications.md`, the bootstrap doc (old-name mentions present only as correctly struck-through historical record), and the `sdp-report-logs-combine`/`sdp-report-log-combined-metrics` rename from the entry immediately below, which was re-verified here and found fully clean with no residue. |
| 1.0.0 | 0 | 2026-07-18 | No version bump (skill/script rename, no behavior change). `sdp-combine-logs` (Level 1 skill, Level 2 skill, and `sdp-shared/scripts/sdp-combine-logs.ps1`) renamed to `sdp-report-logs-combine` (script: `sdp-shared/scripts/sdp-report-logs-combine.ps1`) — user-directed, to group this skill alphabetically with the other `sdp-report-log-*` report-family skills; user confirmed the plural `logs` form (sorting after, not fully interleaved with, the singular `sdp-report-log-*-metrics` skills) after this was flagged as a naming-convention question. Renamed via `git mv` to preserve history: `.claude/skills/`, `sdp-shared/ai-skills/`, and the script file (no skill-eval record existed yet to rename — this skill had not been run through `sdp-evaluate-skill`). Every literal-identifier reference updated to match: the two SKILL.md files' own self-references (L1 frontmatter `name`, tone-invocation `-skillName`, L2 cross-reference paths, halt message, script-invocation path, script-bug-fix pointer), `sdp-report-log-combined-metrics.ps1` (`.SYNOPSIS` and its generated Methodology markdown's dependency mention — new reports only; prior generated report files are left as-is, being immutable per-run snapshots), `sdp-report-log-combined-metrics/SKILL.md` (all six prerequisite/dependency mentions, including its Step 1 halt message), `README.md` (PowerShell Scripts table), `SDP-Workspace-Setup.md` (folder diagram, "Verify sdp- skills" checklist bullet, script-existence check text, permission-entry list), `SDP-Script-Authoring.md` (calling-convention table row, Worked Example heading and prose, permission-entry JSON block, the Rule-1 no-re-interpretation pattern note), `.claude/settings.local.json` (`permissions.allow` entry), `~SDP-Maintenance/~docs/skill-catalogue.md` (catalogue row, Notes bullet — both noting the prior name for provenance). Bootstrap doc has no literal mention of this skill and needed no edit. Left unchanged, deliberately: the data-file naming convention it produces (`combined-log-yyyyMMdd.jsonl`, `combined-logs/`) — unrelated to the skill/script identifier being renamed here — and the one already-generated report file that mentions the old script name in its Methodology section (`sdp-solution-docs/log-reports/combined-metrics/log-report-combined-metrics_20260717-2129.md`), an immutable per-run snapshot, not a living doc. |
| 1.0.0 | 0 | 2026-07-17 | No version bump (user-directed report redesign, `sdp-report-log-hook-metrics` only — no changes to the other three report skills, and no further changes to `sdp-report-log-combined-metrics`, which the user asked to leave alone). Converted every table in this report to a chart: `## Totals` (was a 7-row table) is now two composition bars — Pre/Post split and subagent/main-session split — plus a `**Total events:**` headline and a short truncation text list (not a third bar, since truncation is usually zero for most tools); `## Level Breakdown` and `## Work Item Breakdown` are now horizontal bar charts (color fixed by category identity — signature blue, or muted gray for `(none)` — never by rank, even though rows are still sorted by count for visual ranking); `## Tool Usage` is now a stacked horizontal bar chart (Pre/Post segments, light/full blue); `## Sessions` is now a Gantt-style timeline — extended the script to extract per-`agent_id` sub-spans within each `session_id` (previously only counted subagent *events*, not spans), reusing the exact light-track/dark-fill Meter technique built and fixed for `sdp-report-log-combined-metrics.ps1`'s Rule 1, including labels-on-the-right (carrying that alignment fix forward instead of repeating the original mistake). Since this report is entirely single-source (`hook-log`), every chart uses one signature blue (`#2a78d6`, matching hook-log's color in the combined report) with a light-blue shade for the one real sub-dimension (Pre vs. Post) — no distinct hue per tool/work-item/session was introduced, since those category sets are unbounded and each row already carries its own direct text label. Report regenerated (`log-report-hook-metrics_20260717-2312.md`) — the prior table-based report (`...-2100.md`) was left in place rather than deleted, per this report family's own "each run is a new, immutable file" design (unlike the combined-metrics reports superseded earlier today, which were mid-iteration drafts within the same build). Updated: `sdp-shared/ai-skills/sdp-report-log-hook-metrics/SKILL.md` (Purpose, Step 4 section order and substitutions, Constraints, and the "GFM tables throughout" convention line — this is now the one report in the family that departs from it); `SDP-Script-Authoring.md` (worked example rewritten, new annotated patterns for the signature-hue choice, the Gantt-timeline reuse, and the truncation-as-text-list decision). |
| 1.0.0 | 0 | 2026-07-17 | No version bump (color consistency fix, `sdp-report-log-combined-metrics.ps1` only, same day as the two fixes above). User asked for the Event Category Timeline's "workflow" color to match the Session/Agent Timeline's `workflow-log` color, for cross-chart consistency. Chart 2 actually has two workflow-related categories that only look related by name: `workflow-tone` (a `loop-metrics` entry — a workflow-event *tone* trigger, e.g. `halt.no_progress`, logged via `sdp-tone.ps1`'s event channel) and `workflow-event` (an actual `workflow-log` entry). Only `workflow-event` has a true 1:1 correspondence to Rule 1's `workflow-log` source lane — it is that source's only category — so only `workflow-event` was changed to `#e87ba4` (Rule 1's `workflow-log` color); `workflow-tone` keeps its own distinct color (`#eda100`, unchanged) since it is not workflow-log data and reusing the color there would misattribute it. This freed up a genuine collision: `action` had already been assigned `#e87ba4` (an arbitrary choice, not tied to any cross-chart meaning), so `action` and `workflow-event` swapped colors — `action` now takes `workflow-event`'s old aqua (`#1baf7a`); no new hues introduced, no other category's color changed. Added a Methodology paragraph to the script's own output explaining the `workflow-tone`/`workflow-event` distinction directly in the report, since the naming similarity is a real, demonstrated source of confusion (this exact question is what prompted the fix). Today's report regenerated again (`log-report-combined-metrics_20260717-2129.md`, superseding `...-2119.md`, deleted). |
| 1.0.0 | 0 | 2026-07-17 | No version bump (color fix, `sdp-report-log-combined-metrics.ps1` only, same day as the timeline redesign above). User asked what the Event Category Timeline's `skill-tone` legend item means — clarified it covers every SDP skill's start/end tone marker (any `sdp-tone.ps1 -skillName "X" -event start/end` call, written with `channel=="skill"` in the loop-metrics log), not one specific "tone skill"; confirmed against today's own Event Name Breakdown table, which lists tone entries from several different skills (`sdp-initialize-sdp`, `sdp-evaluate-skill`, `sdp-read-docs`, `sdp-new-claude-terminal`) all under this one category. Separately, the user found `skill-tone`'s color (green `#008300`) hard to distinguish from `tool-post` (blue `#2a78d6`) and asked for rust instead — changed `$rule2ColorMap["skill-tone"]` to `#B7410E`. Only the color value changed; the legend label text ("Skill Tone") and the category's meaning/mapping rule are unchanged. Today's report regenerated again (`log-report-combined-metrics_20260717-2119.md`, superseding `...-2112.md`, deleted). |
| 1.0.0 | 0 | 2026-07-17 | No version bump (bug fix, `sdp-report-log-combined-metrics.ps1` only, same day as the three-timeline redesign above). User caught that the Session/Agent timeline (Rule 1) and the two binned histograms (Rules 2/3) were not horizontally aligned — Rule 1's `viewBox="-110 0 990 …"` put its lane labels in a left-side gutter, which shifted its own timeline's zero-position 110px right of where Rules 2/3's `viewBox="0 0 880 …"` place theirs, so the same wall-clock instant landed at a different pixel column depending on which chart you looked at. Fixed per the user's own suggested approach: moved Rule 1's row labels from the left of the bar (`x="-6"`, `text-anchor="end"`) to the right (`x="886"`, `text-anchor="start"`), and changed its `viewBox` to `"0 0 990 …"` — the timeline portion (x=0 to x=880) now starts at the same coordinate origin in all three SVGs; Rule 1 is simply 110px wider overall (the label gutter moved, not removed). No change to bar/lane geometry, colors, or binning math. Re-verified via the same structural-coordinate inspection used when the timelines were first built (no screenshot tool available). Today's report regenerated again (`log-report-combined-metrics_20260717-2112.md`, superseding `...-2105.md`, deleted). |
| 1.0.0 | 0 | 2026-07-17 | No version bump (user-directed report redesign, `sdp-report-log-combined-metrics` only — no changes to the other three report skills or to `sdp-combine-logs`). Replaced the "Full Event Log" table (a full chronological dump of every combined-log row, 3800+ rows on a typical day) with three SVG timelines sharing one time axis, per user feedback that the table "is not very useful": (1) a session/agent Gantt-style timeline, built only from `hook-log` entries (the only source carrying `session_id`/`agent_id`) — one lane per `session_id`, its lighter "track" spanning the session's full logged period, with a darker full-opacity overlay marking sub-span(s) where a distinct `agent_id` (a dispatched subagent) was active, reusing the dataviz skill's own documented Meter pattern (light track + dark fill, same hue) rather than inventing a new technique; `loop-metrics`/`workflow-log` entries carry no `session_id` and render as their own point-marker pseudo-lanes, colored by source (categorical slots: hook-log=blue, loop-metrics=green, workflow-log=magenta); (2) a binned stacked histogram colored by event category (`tool-pre`/`tool-post` as two shades of one hue since Pre/Post are the same category, not two different things; `skill-tone`/`action`/`workflow-tone`/`workflow-event` each their own hue); (3) the same binning colored by role (`(none)` gets muted gray, never a categorical hue, matching the "absence isn't a vivid category" convention `sdp-report-log-loop-metrics` already established for Off-hours). Bin COUNT is fixed (40) and bin DURATION is computed as the period's span divided by that count — user-specified math so a short report period gets the same column resolution as a long one, rather than a fixed duration like "15 minutes per bin" starving a short period of columns. The session/agent relationship the Gantt logic depends on (every `agent_id` maps to exactly one `session_id`, contiguous own timespan) was verified directly against real hook-log data before implementing, not assumed — a background research pass also confirmed existing documentation only went as far as session-level aggregation, not per-agent timespan extraction, so this was new analysis, not a rediscovery. Colors drawn from the `dataviz` skill's own validated categorical palette (fixed slot order, reused as documented) — no new palette validation needed since no new hues were introduced. Verified structurally against real data (SVG coordinate math, stacking order) since no screenshot tool was available for a literal visual check. Today's report regenerated with the new design (`log-report-combined-metrics_20260717-2105.md`, superseding the same-day `...-1633.md` built under the old table format, deleted). Updated: `sdp-shared/ai-skills/sdp-report-log-combined-metrics/SKILL.md` (Purpose, Step 4 section order and substitutions, a new Constraints bullet on SVG fidelity); `SDP-Script-Authoring.md` (worked example rewritten: new annotated patterns for the Meter reuse, the verified session/agent relationship, fixed-bin-count math, and the Tool Pre/Post shading choice). |
| 1.0.0 | 0 | 2026-07-17 | No version bump (user-directed UX addition across all four report skills, no script changes). Each of `sdp-report-log-loop-metrics`, `sdp-report-log-hook-metrics`, `sdp-report-log-workflow-metrics`, and `sdp-report-log-combined-metrics` gained a new "Open the report" step, immediately after the report file is written and before the final Confirm step: `Invoke-Item '[report_path]'` via the PowerShell tool, opening the file in the user's default associated application. Added consistently: the new step itself, a matching `## Constraints` bullet ("never run Open before the write completes") per this project's established Constraint Phrasing convention (hard rules captured as negative-bounding bullets, not just embedded in step prose), and an `## Outputs` bullet. Step numbers shifted by one in all four skills from this point onward (e.g. loop-metrics' old Step 7 Confirm is now Step 8). No script (`.ps1`) changes — this is purely a skill/LLM-side action, since opening a file isn't something the report-generation scripts do or need to know about. |
| 1.0.0 | 0 | 2026-07-17 | No version bump (new feature, plus a one-time historical data repair — no framework behavior change beyond the repair itself). `sdp-combine-logs` and `sdp-report-log-combined-metrics` created: the former merges one calendar day's `loop-metrics`/`hook-log`/`workflow-log` jsonl files into a single normalized `combined-log-yyyyMMdd.jsonl` under new sibling folder `.sdp-solution-workflow/logging/combined-logs/` (gitignored, added to `.gitignore`); the latter is the fourth report in the family, reading that merged file. Normalization schema (`sdp-combine-logs.ps1`'s `New-Envelope`): every source entry becomes `{timestamp, source, category, role, work_item, phase, event_name, outcome, reason, detail, raw}` — `raw` preserves the complete original entry losslessly regardless of how the normalized fields are derived. Per-source mapping (verified directly against `sdp-tone.ps1`'s actual write shapes before authoring, not assumed): `loop-metrics` has three sub-shapes needing three separate rules — `channel=="skill"` (skill start/end tones) → category `skill-tone`, `event_name` `{skillName}:{event}`; `channel=="event"` (named workflow-event tones) → category `workflow-tone`, `event_name` = the raw `trigger`; entries with `.action` present (written directly by `sdp-state-loop`) → category `action`, `event_name` = the raw `action`, `role`/`work_item`/`outcome` (from `status_after`) carried through, `reason` = `.reason` if present else `.halt_reason` if halted. `hook-log` (one shape) → category `hook-event`, `event_name` `{hook_event_name}:{tool_name}`. `workflow-log` (one shape) → category `workflow-event`, fields carried through directly. `sdp-report-log-combined-metrics.ps1` computes source/category/role/outcome/event-name breakdowns, work-item activity (with a `Sources` column showing which of the three logs contributed), a Concerning Events table (same fixed filter as the workflow-metrics report — outcome in `DIAGNOSIS_BLOCKED`/`GATE_BLOCKED`/`REJECTED`, or `event_name` starting with `halt.` — matched against the unified `event_name` so loop-metrics' own halt-tone triggers are caught too, not just workflow-log's), and a full chronological event table spanning all three original logs. Output reports land in new folder `sdp-solution-docs/log-reports/combined-metrics/`, named `log-report-combined-metrics_[yyyymmdd-hhmm].md`. Both scripts tested against real same-day data before registration. **One-time historical data repair, discovered as a byproduct of testing the combine script against real data and confirmed with the user before acting:** `loop-metrics-20260717.jsonl` was found to contain 8 distinct calendar days of entries (2026-07-10 through 2026-07-17, ~1042 lines total) under one filename — a leftover from the single-file→daily-rotation migration recorded earlier this same day in this changelog, which renamed but never actually split the old file's mixed content. This broke the one-file-per-calendar-day invariant `hook-log`/`workflow-log` already correctly have, and would have caused `sdp-combine-logs` to silently pull a week of extra loop-metrics data into any single day's combine. Verified no shape drift first (all entries, across all 8 days, matched exactly one of the three current `sdp-tone.ps1`/`sdp-state-loop` shapes — nothing to normalize differently). Repaired by splitting the file by each entry's actual date into 8 correctly-named files (`loop-metrics-20260710.jsonl` through `loop-metrics-20260717.jsonl`, the last overwriting the mixed file in place), verified lossless (1042 lines in, 1042 out, exact match) via a pre-split backup (`loop-metrics-20260717.jsonl.pre-split-backup`, kept alongside the repaired files — gitignored, not deleted, for the user's own peace of mind). Today's `combined-log-20260717.jsonl` (built once before the repair, during testing) was regenerated afterward so it reflects only today's real loop-metrics entries. Registered (both the new skill pair and the `combined-logs/` folder): `.gitignore` (new `combined-logs/` entry); `README.md` (PowerShell Scripts table); `SDP-Workspace-Setup.md` (folder diagram — `combined-logs/` added as a fourth logging sibling with its on-invocation-not-passive distinction called out, Setup Checklist Step 2, "Verify sdp- skills" list, script-existence text, permission-entry list); `SDP-Script-Authoring.md` (calling-convention rows, two new Worked Example sections); `.claude/settings.local.json` (two `permissions.allow` entries); `~SDP-Maintenance/~docs/skill-catalogue.md` (two new rows, not-yet-evaluated — Total updated 33→35, new Notes bullet). |
| 1.0.0 | 0 | 2026-07-17 | No version bump (new feature — two companion report skills, no changes to existing behavior). `sdp-report-log-hook-metrics` and `sdp-report-log-workflow-metrics` created, using `sdp-report-log-loop-metrics` as an architectural template (L1 shim + L2 assembler + script triad, same script/skill boundary philosophy — everything derivable from the jsonl alone via a fixed rule is script work, everything else is skill/LLM work) but each adapted to its own source log's actual data shape rather than reusing the loop-metrics bucket/halt-window logic, which doesn't apply to either: `hook-log-*.jsonl` is already-complete raw per-tool-call telemetry (no start/end pairing to infer), and `workflow-log-*.jsonl` is an already-semantic narrative (`reason` is free text authored at logging time) — neither has ambiguous breadcrumbs to resolve, so each skill has only one or two **(judgment)** spots (a short Summary paragraph; Concerning Events commentary for workflow-metrics) versus loop-metrics' many. Naming: the report family is `hook-metrics`/`workflow-metrics` — matching `loop-metrics`'s own convention — deliberately distinct from the source data's own `hook-log`/`workflow-log` naming (`hook-log-*.jsonl`, `hook-logs/`, etc., all unchanged, unrelated to this feature); the user corrected the initial `hook-log`/`workflow-log` skill-naming choice mid-build to this scheme before any file left draft state, so every artifact below reflects it directly rather than via a rename pass. New scripts: `sdp-shared/scripts/sdp-report-log-hook-metrics.ps1` (totals, level breakdown, tool-usage table, `session_id`-grouped sessions table, work-item breakdown) and `sdp-shared/scripts/sdp-report-log-workflow-metrics.ps1` (trigger/role/outcome breakdowns, full chronological events table, a fixed-filter Concerning Events table — outcome in `DIAGNOSIS_BLOCKED`/`GATE_BLOCKED`/`REJECTED` or trigger starting with `halt.`, surfaced as candidates not verdicts — and work-item/phase activity tables); both tested against real same-day log data before and after the naming correction. New skill pairs: `.claude/skills/sdp-report-log-hook-metrics/` + `sdp-shared/ai-skills/sdp-report-log-hook-metrics/`, and the `-workflow-metrics` equivalents. Output reports land in new sibling folders `sdp-solution-docs/log-reports/hook-metrics/` and `sdp-solution-docs/log-reports/workflow-metrics/`, named `log-report-hook-metrics_[yyyymmdd-hhmm].md` / `log-report-workflow-metrics_[yyyymmdd-hhmm].md` — same `log-reports/` container `sdp-report-log-loop-metrics` already established. Registered: `README.md` (PowerShell Scripts table, grouped adjacent to the loop-metrics row rather than strict alphabetical order — not itself corrected here, a pre-existing minor ordering artifact from the loop-metrics rename); `SDP-Workspace-Setup.md` (folder diagram, "Verify sdp- skills" checklist bullet, script-existence check text, permission-entry list); `SDP-Script-Authoring.md` (calling-convention table rows, two new Worked Example sections, each noting the hook-log/hook-metrics and workflow-log/workflow-metrics naming split explicitly); `.claude/settings.local.json` (two `permissions.allow` entries); `~SDP-Maintenance/~docs/skill-catalogue.md` (two new rows, marked not-yet-evaluated — `sdp-evaluate-skill` has not been run against either — Total updated 31→33, new Notes bullet). Bootstrap doc needed no edit: it makes no path- or skill-name-specific claim about hook-log/workflow-log reporting that this feature contradicts. |
| 1.0.0 | 0 | 2026-07-17 | No version bump (user-directed report-output relocation/rename, no behavior change beyond the path/filename). `sdp-report-log-loop-metrics`'s generated reports moved from `sdp-solution-docs/reports/loop-metrics/` to `sdp-solution-docs/log-reports/loop-metrics/` — `log-reports/` is a new generic container for future log-derived report types, not specific to loop-metrics. Report filename prefix changed from `loop_metrics_report_` to `log-report-loop-metrics_`, keeping the existing `[yyyymmdd-hhmm]` creation-timestamp suffix unchanged (the user's first-pass ask was a date-only `YYYYMMDD` suffix, but the two existing reports — both dated 2026-07-12, covering different periods, 847 vs 963 source lines — would have collided under that scheme; user chose to keep the `-HHMM` component when presented with the conflict). Moved via `git mv` (2 `.md` reports + their 2 companion `.pdf` files under a sibling `MD-to-PDF/` folder, user-maintained, not part of any SDP skill/config) to preserve history; empty leftover `sdp-solution-docs/reports/` tree (including a stray pre-existing empty `reports/log-reports/` directory, untracked, unrelated to this change) removed. Updated: `sdp-shared/ai-skills/sdp-report-log-loop-metrics/SKILL.md` Inputs section (default output path) and Step 1 sub-step 2 (folder-exists-before-writing check) — the two only places this path was asserted. `sdp-report-log-loop-metrics.ps1` needed no edit: the script never writes or references the report output path at all — Step 6 (report assembly and file write) is entirely skill/LLM-side. Bootstrap doc has no mention of this path and needed no edit. |
| 1.0.0 | 0 | 2026-07-17 | No version bump (skill/script rename, no behavior change). `sdp-loop-metrics-report` (Level 1 skill, Level 2 skill, and `sdp-shared/scripts/sdp-loop-metrics-report.ps1`) renamed to `sdp-report-log-loop-metrics` (script: `sdp-shared/scripts/sdp-report-log-loop-metrics.ps1`) — user-directed, no rationale beyond the new name. Renamed via `git mv` to preserve history: `.claude/skills/`, `sdp-shared/ai-skills/`, the script file, and its skill-eval record (`~SDP-Maintenance/~sdp-shared/~skill-evals/`). Every literal-identifier reference updated to match: the two SKILL.md files' own self-references (L1 frontmatter `name`, tone-invocation `-skillName`, L2 cross-reference path, halt message, script-invocation path, script-bug-fix pointer), `README.md` (PowerShell Scripts table), `SDP-Workspace-Setup.md` (folder diagram, "Verify sdp- skills" checklist bullet, script-existence check, permission-entry list), `SDP-Script-Authoring.md` (calling-convention table row, Worked Example heading and prose, permission-entry JSON block), `SDP-Tone-Notifications.md` (dispatch-path pointer), `.claude/settings.local.json` (`permissions.allow` entry), `~SDP-Maintenance/~docs/skill-catalogue.md` (refresh-note list, catalogue row, Summary-by-Verdict list), `~SDP-Maintenance/~docs/sdp-create-banner-adoption-tracker.md` (survey-exclusion list, Tier-4 table row), and two other skills' eval files that cited it in passing (`sdp-workspace-setup-eval.md`, `sdp-initialize-sdp-eval.md`). Bootstrap doc's four literal mentions (main Loop-orchestrated-dispatch-path paragraph plus three dated Correction/Addition blockquotes) corrected via strikethrough + a new dated Correction appended immediately after, per Append-Only Discipline — not rewritten in place. Left unchanged, deliberately: the report *output* filename convention (`loop_metrics_report_[yyyymmdd-hhmm].md`, underscore-separated, no `sdp-` prefix) and the input log naming (`loop-metrics-*.jsonl`, `loop-metrics-yyyyMMdd.jsonl`) — both are data-file naming conventions, unrelated to the skill/script identifier being renamed here. |
| 1.0.0 | 0 | 2026-07-17 | No version bump (documentation-only — closing a gap found during a same-day doc-sufficiency review, not a behavior change). Two logging mechanisms added earlier today (see the two entries below) had shipped in code and in the bootstrap doc's own dated Addition blockquotes, but had zero footprint in `SDP-Changelog.md`, `SDP-Script-Authoring.md`, or `README.md`'s PowerShell Scripts table — a review specifically requested to check whether `sdp-create-banner` and the new logging mechanisms had "the right level of info" in README.md and internal docs surfaced this alongside 3 similar gaps for the banner feature. Closed: `SDP-Script-Authoring.md` (calling-convention table rows plus full Worked Example sections for `sdp-hook-log.ps1` and `sdp-workflow-log.ps1`, and — closing the banner-side gap — for `sdp-create-banner.ps1`, which had never gotten one despite being registered elsewhere); `SDP-Workspace-Setup.md` (Setup Checklist Step 2 now names `hook-logs/`/`workflow-logs/` alongside `loop-logs/`, which the folder diagram already had but the checklist had silently dropped; new "Create/Copy `sdp-hook-log-tools.json`" checklist item, which had no creation step anywhere before now); `SDP-Skill-Authoring.md` (new "Convention: Chat-Facing Halt/Warning/Verdict Messages" section — the skill's *existence* was documented as an L1-only exception, but no doc told a future skill author to actually *use* it for new halt/warning/verdict messages); bootstrap doc (`SDP_Sapient-Driven-Principles_v1.0.0.md`'s Halt Behavior Contract section — the framework's own authoritative spec still showed the pre-`sdp-create-banner` raw-text form; corrected via strikethrough + a dated blockquote, per Append-Only Discipline, not rewritten in place). This changelog entry itself is part of the same closure — see the two entries immediately below for the logging mechanisms' own record. Root cause, same pattern as the reactive-registration lesson already on record two rows below: a feature's own creation session logged the mechanism but not its cross-doc footprint; the fix each time is the same standing checklist item — "register alongside the nearest existing precedent" — this is now the third time this exact gap class has been closed reactively rather than caught at authoring time. |
| 1.0.0 | 0 | 2026-07-17 | No version bump (feature addition, logged retroactively — see the entry above for why). Semantic workflow-event logging added: `sdp-shared/scripts/sdp-workflow-log.ps1`, called directly (not via a hook) by `sdp-coordinator`, `sdp-worker`, `sdp-reviewer`, and internally by `sdp-gate-review-finalize.ps1` at points a hook cannot reach — a hook only sees `tool_name`/`tool_input`/`tool_output`, not *why* a decision was made. Writes one JSON line per call to `.sdp-solution-workflow/logging/workflow-logs/workflow-log-<local-yyyyMMdd>.jsonl` — a sibling to `loop-logs/` and `hook-logs/`, same one-file-per-local-day shape and 190-day retention sweep on first write of each new day. Correlates with `hook-logs/` by `work_item`, not `session_id` — Claude Code does not expose a generic session identifier to an agent-invoked script (confirmed against the Claude Code docs before this script was written); SDP's own Role Separation invariant (one role, one work item, per session) makes `work_item` an adequate join key regardless. Shares its named-event trigger vocabulary with `SDP-Tones.json`'s `events` table where applicable. See `SDP-Workspace-Setup.md` for the folder diagram entry (already present since this script's creation) and the entry above for what was missing until now. |
| 1.0.0 | 0 | 2026-07-17 | No version bump (feature addition, logged retroactively — see the top entry for why). Hook-based tool-call logging added: `sdp-shared/scripts/sdp-hook-log.ps1`, registered as an async `PreToolUse`/`PostToolUse` command hook (no matcher — fires for every tool call) in `.claude/settings.local.json`, gated per-tool by new `.sdp-solution-workflow/script-support/sdp-hook-log-tools.json` (level `TIMING`/`AUDIT`/`HUMAN_WAIT`/`DEBUG`, `logPre`/`logPost`, `includeSubagentOrigin`, plus a `defaultForUnlistedTools` tripwire for tools added later). Writes one JSON line per logged event to `.sdp-solution-workflow/logging/hook-logs/hook-log-<local-yyyyMMdd>.jsonl` — a sibling to `loop-logs/`, same one-file-per-local-day shape, 190-day retention sweep on first write of each new day. Raw per-tool-call telemetry, keyed by `session_id` (distinguishes concurrent Claude Code windows) and `agent_id` (present only inside a dispatched subagent). Async hooks have their exit code/stdout ignored by Claude Code (confirmed against the Claude Code hooks reference before this script was written), so this mechanism can never block, deny, or influence the tool call it observes. No reporting skill reads this log yet — a forward item, mirrored by `sdp-workflow-log.ps1` below having none either. See `SDP-Workspace-Setup.md` for the folder diagram entry (already present since this script's creation) and the top entry for what was missing until now. |
| 1.0.0 | 0 | 2026-07-17 | No version bump (user-directed `sdp-create-banner` adoption pass — visual-standardization only, no behavior change). Surveyed all 29 candidate skills (`sdp-initialize-sdp` and `sdp-create-banner` itself excluded by design) for chat-facing halt/warning/confirmation/gate-verdict messages worth banner treatment, producing a 4-tier categorized list; user approved Tier 1 (23 skills' own halt/warning/verdict messages) plus Tier 2 (two shared framework-wide patterns — the generic L1 "L2 SKILL.md not found" halt, and the "Halted — [reason]. Resolve this condition and run COORDINATOR to resume." Halt Behavior Contract text) for adoption. Implemented via one independent edit agent per skill, followed by one independent fresh-context review agent per skill verifying against that skill's approved message list, with fix-and-re-review retries capped at 3 total edit attempts per skill — 22/23 skills converted cleanly through that process (`sdp-auto`, `sdp-cancel-auto`, `sdp-coordinator`, `sdp-create-prompt`, `sdp-evaluate-skill`, `sdp-gate-review`, `sdp-loop-prep`, `sdp-new-concept-intake`, `sdp-pre-work-verify`, `sdp-read-docs`, `sdp-reviewer`, `sdp-run-prompt`, `sdp-solution-coordinator`, `sdp-solution-create-prompt`, `sdp-solution-reviewer`, `sdp-solution-run-prompt`, `sdp-solution-worker`, `sdp-source-coverage-check`, `sdp-standards-setup`, `sdp-state-loop`, `sdp-state-loop-start`, `sdp-worker`, `sdp-workspace-setup`'s first two of three banners); `sdp-workspace-setup`'s third banner (Step 3's conditional concept-intake-hand-off extension) exhausted the 3-attempt cap with the exact remaining defect precisely diagnosed by its reviewer — a `row=0,1,2,3` extension plus a separately-appended stray `row=3 row: Intake | ...` fragment, producing two `row=` directives in one invocation, which `sdp-create-banner.ps1` hard-fails on ("only one row= directive is supported per invocation") — fixed directly by the orchestrating session (a one-line instruction rewrite dropping the stray `row=3` prefix) rather than spending a fourth subagent attempt on an already-exact diagnosis. Full per-skill agent/review counts and notes recorded in new tracking doc `~SDP-Maintenance/~docs/sdp-create-banner-adoption-tracker.md`. Deliberately out of this pass's scope: Tier 3/4 skills from the survey (`sdp-doc-review`, `sdp-loop-metrics-report`, `sdp-new-claude-terminal`, `sdp-phase-rightsizing-check`, `sdp-solution-read-docs`, `sdp-tone` — no qualifying candidate, or judged not worth it per the survey's stated criteria) were left untouched, including the two Tier-2 shared patterns within them — extending those patterns to skills outside the user's approved Tier-1 scope was flagged to the user as a scope question, not decided unilaterally. |
| 1.0.0 | 0 | 2026-07-17 | No version bump (checklist/registration completeness — no behavior change; a same-day gap found and closed during an end-of-session review). `sdp-create-banner`'s conversion to a fully-scripted, L1-only skill (logged separately above) left it unregistered in the same places `sdp-tone`/`sdp-new-claude-terminal` already appear as L1-only precedents. Closed: `.claude/settings.local.json` (`permissions.allow` entry for `sdp-create-banner.ps1`, without which subagent PowerShell calls to it would be silently blocked); `SDP-Skill-Authoring.md` ("Exception: Level-1-only, fully-scriptable skills" — "two current examples" → "three," adding it); `SDP-Workspace-Setup.md` ("Verify sdp- skills" checklist bullet and the "Verify script permissions" enumerated list); `README.md` (three tables: skill tier assignments, Shared skills inventory, PowerShell Scripts); `SDP-Script-Authoring.md` (calling-convention table row); `SDP-Workspace-Setup.json` (two new `file-exists` preflight checks — the L1 shim and the icon-registry data file). Root cause: the registration sweep was done reactively (a follow-up ask), not as part of the original conversion step — worth treating "register alongside the nearest existing L1-only precedent" as a standing checklist item for any future script conversion, not a separate afterthought. |
| 1.0.0 | 0 | 2026-07-17 | No version bump (user-directed config relocation). `SDP-Terminal-Sessions.json` (POC registry for `sdp-new-claude-terminal` — see that skill's own eval and `~SDP-Maintenance/~docs/terminal-orchestration-exploration.md` for the not-yet-adopted multi-window architecture it supports) moved from the solution root to `.sdp-solution-workflow/script-support/`, alongside `SDP-Tones.json` and `sdp-create-banner-icons.json`. `sdp-new-claude-terminal.ps1`'s `$registryPath` updated (its only reference to the file's location); `.NOTES` Reads/Writes block updated to match. `.claude/skills/sdp-new-claude-terminal/SKILL.md` needed no edit — it never asserts a literal path, only invokes the script. `README.md` tree updated (removed from the flat solution-root list, added under `script-support/`). Left untouched, deliberately: `SDP-Config.json`'s `newTerminals` config (no path reference to the registry file), `SDP-Workspace-Setup.md` (names the file once, without asserting a path — this POC feature was never added to the official setup checklist or folder diagram to begin with), `terminal-orchestration-exploration.md` (a point-in-time exploration record — its value is as a snapshot of that session's reasoning, not a living reference to keep current), and the skill's own eval file (append-only). Verified by reading the registry from its new path with the script's own `Get-Content -Raw -Encoding UTF8 | ConvertFrom-Json` pattern — all 6 existing instance entries parse correctly; did not invoke the script directly for this verification since a real (non-`-whatIf`) run spawns an actual terminal window as a visible side effect, and `-whatIf` exits before reaching the registry code entirely. |
| 1.0.0 | 0 | 2026-07-17 | No version bump (same-day revision of the metrics-log restructure below — user reconsidered the rotation trigger before it shipped in any released form). `loop-metrics-*.jsonl` rotation changed from "new file per `sdp-state-loop-start` invocation" (`loop-metrics-yyyyMMddHHmm.jsonl`) to pure calendar-day rotation, independent of any workflow action (`loop-metrics-yyyyMMdd.jsonl`) — the script that writes creates the current day's file if it doesn't exist yet, no dedicated creation step needed. `sdp-tone.ps1`'s `Resolve-MetricsPath` simplified from a `Get-ChildItem`/`Sort-Object` "most recent file" lookup to directly computing today's filename. `sdp-state-loop` Step 6 similarly simplified — dropped the lookup sub-step entirely, `Add-Content` now targets a directly-computed `loop-metrics-$(Get-Date -Format 'yyyyMMdd').jsonl` path (its own file-creation-on-first-write behavior is unchanged and sufficient). **`sdp-state-loop-start`'s Step 2 sub-step 3 (the new-file-creation step added earlier today) was fully reverted** — the file is now byte-identical to its pre-restructure version; that skill no longer touches metrics files at all. `sdp-loop-metrics-report`'s picker now presents plain dates instead of readable timestamps (the filename has no time component to derive one from). Existing file `loop-metrics-202607101839.jsonl` (created earlier today under the since-abandoned trigger) renamed to `loop-metrics-20260717.jsonl` — a straight rename, its mixed 2026-07-10-through-today content was not split by day. Updated for consistency: `.gitignore` comment intent unchanged (folder-level ignore already covers any filename pattern), `SDP-Workspace-Setup.md` (folder diagram, Setup Checklist Step 2 bullet), `SDP-Tone-Notifications.md` (Metrics side-effect, Constraints-style bullet list). Bootstrap doc's `Correction — 2026-07-17` blockquote from the original restructure (below) was itself partially superseded via strikethrough + a second same-day correction appended immediately after it — not edited in place, per Append-Only Discipline, even though both corrections landed within the same session. |
| 1.0.0 | 0 | 2026-07-17 | No version bump (user-directed metrics-log restructure). `loop_metrics.jsonl` — previously one continuously-growing file at the solution root — replaced with one dated `loop-metrics-yyyyMMddHHmm.jsonl` file per "body of work," under new folder `.sdp-solution-workflow/logging/loop-logs/`. Existing file (1030 lines, spanning 2026-07-10 through today) moved and renamed to `loop-metrics-202607101839.jsonl` per its first entry's timestamp. Trigger for a new file is `sdp-state-loop-start` (a new automated loop run) — chosen over per-session (`sdp-initialize-sdp`, out of scope for edits) or fully-manual after weighing fragmentation risk against automatic coverage. Writers always target the most recently dated file: `sdp-tone.ps1`'s `Write-MetricsLine` now resolves it fresh on every write via a new `Resolve-MetricsPath` function (bootstraps a file itself if none exists yet, e.g. tone activity before any loop has started this workspace's lifetime); `sdp-state-loop` Step 6 gained a `Get-ChildItem`/`Sort-Object` sub-step before its `Add-Content` call, with the same create-if-none fallback. `sdp-state-loop-start` Step 2 gained a new sub-step 3 that creates the fresh dated file (its Constraints/Outputs updated — it is no longer a zero-direct-file-write skill). `sdp-loop-metrics-report` (skill + script) reworked for multi-file reads: Step 1 now resolves *which run's file* first — a named run from the request, or (the common case, no date given) an AskUserQuestion picker over the four most recent runs, labeled by readable start time, before falling through to the existing `-Date`/`-StartDate`/`-EndDate`/`-Days` filters (unchanged, now scoped *within* the selected file); the resolved path is always passed via the script's pre-existing `-JsonlPath` parameter, which itself gained a new default (most-recently-dated file, for direct invocation outside the skill) in place of the old fixed-path default. Updated: `.gitignore` (`.sdp-solution-workflow/loop_metrics.jsonl` → `.sdp-solution-workflow/logging/loop-logs/`), `SDP-Workspace-Setup.md` (folder diagram, Setup Checklist Step 2 bullet), `SDP-Tone-Notifications.md` (Metrics side-effect, Constraints-style bullet list), `SDP-Script-Authoring.md` (worked example prose, annotated-patterns table). Bootstrap doc's active (non-superseded) "Loop-orchestrated dispatch path" bullet — the one place this file's old fixed location was asserted outside already-dead SKILLS CHECK blocks — corrected via strikethrough + a dated `> **Correction — 2026-07-17:**` blockquote, per Append-Only Discipline; not rewritten in place. |
| 1.0.0 | 0 | 2026-07-17 | No version bump (user-directed script conversion + config relocation). `sdp-create-banner` converted from L1+L2 to L1-only (no version bump for `sdp-create-banner` itself — it has no version field): all tokenizing, icon resolution, and width-aware row/border construction moved into new `sdp-shared/scripts/sdp-create-banner.ps1`; `sdp-shared/ai-skills/sdp-create-banner/SKILL.md` deleted; `.claude/skills/sdp-create-banner/SKILL.md` rewritten to be fully self-contained (invocation grammar, worked examples, icon-registry pointer). New solution-root folder `.sdp-solution-workflow/script-support/` holds data files read internally by scripts (invisible to agent context — the calling skill never reads them, only the script does): `sdp-create-banner-icons.json` (54-entry icon registry, ported from the deleted L2's table) and, in a follow-up same-day move, `SDP-Tones.json` (relocated from the solution root). `sdp-tone.ps1` updated to read the new path; `SDP-Config.json`'s `tones` pointer text unchanged (still names `SDP-Tones.json`, no path asserted there). Updated for the `SDP-Tones.json` move: `SDP-Workspace-Setup.json` (`file-exists` check path), `SDP-Workspace-Setup.md` (folder diagrams ×2, Step 1 copy list, SKILLS CHECK mention, "Create `SDP-Tones.json`" checklist step, per-project manifest guidance, Template section), `SDP-Tone-Notifications.md` (Quick Reference table, Overview), `SDP-Script-Authoring.md` (Solution-root scripts category reworded — root self-resolution is the defining trait, not the file sitting literally at the top level; `sdp-create-banner.ps1` added as a third example), `sdp-workspace-setup/SKILL.md` ("framework already present" check no longer includes `SDP-Tones.json` — it is created during setup, not copied in), `README.md` (tree), `QuickStart.md` (copy list, per user instruction — the file no longer needs to be listed there). Bootstrap doc (`SDP_Sapient-Driven-Principles_v1.0.0.md`) needed no edit: its only literal-path mentions of `SDP-Tones.json`'s former "workspace root" location are inside already-struck-through, already-superseded SKILLS CHECK blocks; its active PREFLIGHT CHECK mentions name the file without asserting a path. |
| 1.0.0 | 0 | 2026-07-11 | No version bump (checklist completeness — no behavior change). `SDP-Workspace-Setup.md`'s "Verify sdp- skills" Setup Checklist bullet was stale: it enumerated only 7 of the 22 real L1+L2 skill pairs, omitting `sdp-auto`, `sdp-cancel-auto`, `sdp-doc-review`, `sdp-evaluate-skill`, `sdp-gate-review`, `sdp-solution-read-docs`, `sdp-standards-setup`, `sdp-state-loop`, `sdp-state-loop-start`. Rewritten to enumerate the full current inventory (`.claude/skills/` vs `sdp-shared/ai-skills/` diffed directly), grouped as core/loop-automation/solution-level skill pairs, plus the two Level-1-only fully-scriptable skills (`sdp-tone`, `sdp-new-claude-terminal` — no Level 2 file by design). `SDP-Skill-Authoring.md` gains a new "Exception: Level-1-only, fully-scriptable skills" subsection documenting that pattern, since the checklist rewrite pointed to it and the pattern wasn't actually written down anywhere before now. |
| 1.0.0 | 0 | 2026-07-11 | No version bump (docs catching up to an already-shipped code fix). `sdp-gate-review-gpg-check.ps1` was fixed the same day (commit `1558cec`) to resolve `standards/GenericProjectGuidlines_[version].md` against the solution root instead of `$workspaceRoot` (the project root) — a solution-level resource was being looked up under the wrong root, false-halting gate review in any multi-project solution. `SDP-Script-Authoring.md` updated: new "Mixed-root scripts" note in the Workspace Root Resolution section (a project-scoped script that also touches a solution-level resource must resolve that resource against an independently-derived solution root, never `$workspaceRoot`), the three `sdp-gate-review-*.ps1` scripts added to the calling-convention table, and a new worked example for `sdp-gate-review-gpg-check.ps1`. `sdp-shared/skill-evals/sdp-gate-review-eval.md` gets a dated Post-Implementation Correction appended (append-only — the original 2026-07-07 Implementation Record text is unchanged) noting its "none self-resolve" line is now partially superseded. |
| 1.0.0 | 0 | 2026-07-11 | No version bump (docs catching up to already-shipped code). New solution-root append-only log `.sdp-solution-workflow/loop_metrics.jsonl` (gitignored): `sdp-state-loop` Step 6 appends one JSON line per fire (`timestamp`/`project`/`action`/`work_item`/`role`/`status_before`/`status_after`/`halted`/`halt_reason`); `sdp-tone.ps1` appends one JSON line per real (non-`-whatIf`) invocation on both its skill-tone and workflow-event channels, bracketing subagent work independently of the loop's own fire log. New `sdp-loop-metrics-report` skill (L1 + L2 + `sdp-loop-metrics-report.ps1`) reads this file on manual invocation and produces a report — six-bucket time accounting (Productive/Halt resolution/Off-hours/Unproductive/User-interrupted/Idle), both SVG time-flow bars, halt/halt-resolution/off-hours/user-interrupted interval tables with each halt window's interleaved sub-timeline, task-by-task outcomes, loop-fire breakdown, and a Phase/Work Covered table — written to `sdp-solution-docs/reports/loop-metrics/`. Docs updated to reflect the already-implemented code: bootstrap doc's Loop-orchestrated dispatch path bullets; `SDP-Workspace-Setup.md` folder diagram (`loop_metrics.jsonl`, `sdp-loop-metrics-report.ps1`) and Setup Checklist (skill + script + permission entries); `SDP-Script-Authoring.md` calling-convention table and new `sdp-loop-metrics-report.ps1` worked example; `SDP-Tone-Notifications.md` Overview and Constraints documenting `sdp-tone.ps1`'s metrics side-effect. `.gitignore` and `.claude/settings.local.json` permission entry were already in place. |
| 1.0.0 | 0 | 2026-07-06 | No version bump (user-directed). Superpowers integration deepened across bootstrap and skills. Bootstrap: `~SDP-Maintenance` workspace-detection guard added to Agent Read Instructions — if the folder exists at the solution root, the agent confirms this is the SDP framework's own development workspace and skips the new-project setup wizard; SP-vs-SDP terminology callout added to the Superpowers Plugin Integration section (SP = third-party Superpowers plugin by Jesse Vincent; SDP = this workflow system); integration table expanded from 5 to 12 rows with full skill-level policy: `/verification-before-completion` (WORKER, required before the Completed blockquote), `/receiving-code-review` (WORKER, required on REJECTED-task rework), `/dispatching-parallel-agents` (WORKER/REVIEWER, optional for 2+ independent reads), `/requesting-code-review` (REVIEWER, optional thinking aid before dispatching verification sub-agents), `/finishing-a-development-branch` (COORDINATOR, optional when all phase tasks are VERIFIED and the gate has passed), `/writing-plans` and `/subagent-driven-development` (prohibited in all roles — conflicts with or is redundant with the SDP phase-document/session model), `/using-git-worktrees` (prohibited for WORKER — SDP isolation is already at the subagent-session level). `sdp-worker` adds Step 5a invoking `/verification-before-completion` before recording completion, plus `/receiving-code-review` and `/dispatching-parallel-agents` per the table above. `sdp-reviewer` adds `/requesting-code-review` and `/dispatching-parallel-agents` as optional thinking aids; sub-steps renumbered. `sdp-coordinator` adds a brainstorming-capture check for Phase 1/2 sessions and invokes `/finishing-a-development-branch` on gate pass. `sdp-run-prompt` refactored to delegate project resolution, prompt parsing, and skill selection to a new `sdp-run-prompt.ps1` script; Level 2 SKILL.md reduced to orchestration only. New `sdp-shared/skill-evals/` folder added with one evaluation report per SDP skill. Obsolete `research/` files removed (portability-analysis PDF, SDP-ISSUE doc, SP-SDP integration proposal, meta-skill draft). |
| 1.0.0 | 0 | 2026-06-24 | Multi-project solution layout and `sdp-project_` naming convention fully implemented. New `SDP-Solution.json` registry at solution root (`schema_version`, `solution_name`, `last_active_projects`, `active_solution_task`, `projects`). New solution-root folders: `.sdp-solution-workflow/` (state + sessions), `sdp-solution-docs/` (solution prompt + user notes), `sol-shared/` (placeholder). Five new solution-level skill pairs (L1 + L2): `sdp-solution-coordinator` (cycle-sync invariant, cascade detection), `sdp-solution-worker` (child task decomposition, parallel/sequenced dispatch), `sdp-solution-reviewer` (cross-project integration check, cascade on rejection), `sdp-solution-run-prompt`, `sdp-solution-create-prompt`. Ten existing skills updated for three-level project resolution (session `Project:` field → sentinel `projects=` → `SDP-Solution.json` `last_active_projects[0]`): `sdp-coordinator`, `sdp-worker`, `sdp-reviewer`, `sdp-state-loop`, `sdp-state-loop-start`, `sdp-run-prompt`, `sdp-create-prompt`, `sdp-read-docs`, `sdp-pre-work-verify`. `sdp-read-docs` now uses two-pathway loading model: Pathway 1 scans solution root for `SDP_Sapient-Driven-Principles_v*.md` (highest version, never via Document List); Pathway 2 reads active project's `SDP-Document-List.json`. `SDP-Workspace-Setup.md` updated with solution root and per-project folder diagrams, two-level setup checklist, `SDP-Solution.json` templates. Bootstrap doc updated: Workspace Setup pointer, Setup Checklist, PREFLIGHT CHECK `-workspaceRoot .\[resolved_project]`, step 6a resolution reference. `SDP-Workspace-Setup.json` per-project: `sdp-shared/` file-exists checks removed (now solution-root concerns). `SDP-Config.json` confirmed at solution root only. |
| 0.9.6 | 0 | 2026-06-23 | New skills: `sdp-doc-review` (COORDINATOR sub-step 1b — before dispatching any task, verifies each staged phase document's `sdp_doc_review` key; dispatches a doc review session for any unreviewed doc to lock in open decisions and implementation blockers before WORKERs are dispatched) and `sdp-gate-review` (GATE_REVIEWER session skill for the Phase Gate Procedure — reads the completed phase document, appends a Gate Verdict blockquote, and updates `phase_gate.status` in `state.json`). Implementation Loop step 1b and Phase Gate Procedure step 2 updated to reference these skills. `sdp-coordinator` updated to include the DOC REVIEW CHECK. New `sdp-preflight.ps1` manifest-driven workspace check engine: replaces hand-enumerated SKILLS CHECK and GPG CHECK steps across COORDINATOR and Phase Gate with a single script call that validates every deterministic precondition against `SDP-Workspace-Setup.json` and emits a structured JSON envelope — COORDINATOR steps 0, 0b superseded by strikethrough. `sdp-project_` folder convention design spec finalized (superseded design spec docs removed from `docs/`). Prep for major project folder restructure. |
| 0.9.5 | 0 | 2026-06-21 | `sdp-create-prompt` hybrid revised to **script-as-writer (Option 1)** — no version bump (user-directed). The script (`sdp-create-prompt.ps1`) now writes `sdp-docs/00_prompt.txt` directly in single-option form; the LLM no longer reads the temp file and re-emits the sections verbatim on the normal path (it added no non-deterministic value — the script's section strings are already the final content). Level 2 SKILL.md normal-path tool calls reduced 3 → 1; the LLM retains only the Option A context gate, the rare two-option overwrite (needs conversational context the script cannot see), and halt/error handling. Success status JSON gains `promptFile`. Two encoding hardening fixes surfaced by making the script the writer: (1) `00_prompt.txt` is written UTF-8 **without BOM** so the first-line `[sdp-prompt …]` sentinel stays matchable by `sdp-state-loop` — a BOM before `[` would break sentinel detection and cause a spurious GENERATE; (2) section headers build the em-dash from `[char]0x2014` and all JSON/text inputs are read with `-Encoding UTF8`, because Windows PowerShell 5.1 parses a no-BOM `.ps1` source and reads a no-BOM data file as the system ANSI codepage, corrupting multibyte characters (em-dash in headers, or in `project_name`/`halt_reason`) on both the source and read paths. Pester suite extended (prompt-file written, BOM-free sentinel, headers, halt writes nothing) and `docs/2026-06-19-sdp-create-prompt-hybrid-design.md` updated. |
| 0.9.5 | 0 | 2026-06-20 | Dispatch-authoring guard for the loop-owned `eval_cycle_attempts` field. `sdp-coordinator` skill (Step 5.2 prohibition, Step 5.3 mandatory sentinel `role`, Constraints bullet) and bootstrap Stuck-Loop Detection now forbid any dispatch file (`session-NNN.md` / `sdp-docs/00_prompt.txt`) from instructing WORKER or REVIEWER to set, seed, or increment `eval_cycle_attempts` (owned solely by `sdp-state-loop`), and require the sentinel `role` field. Allowed dispatch state-writes enumerated per role: WORKER → `status`/`last_session`/`last_updated`; REVIEWER → `eval_cycles`/`status`. Root cause: COORDINATOR agents authored loop-owned-field write instructions in dispatch files (observed downstream in CapEx Watch sessions 089/092/094/095); correctly-guarded WORKERs refused, and a missing sentinel `role` corrupted attempt accounting. **Also in 0.9.5 — Tone/tune notification framework:** tone config split out of `SDP-Config.json` into new `SDP-Tones.json` (flattened; `palette`/`assignments` retained for skill start/end; new `sequences`, `profiles`, `events`). `sdp-tone.ps1` gains a `-trigger` channel that plays multi-note sequences with repeat/gap profiles, plus a `-whatIf` test switch (resolves the playback plan to JSON without sound). New `SDP-Tone-Notifications.md` companion doc (music reference moved out of the `sdp-tone` Level 1 SKILL.md to cut hot-path context). Workflow-event triggers wired: `sdp-state-loop` (api.error_detected, halt.no_progress ×5, halt.generic ×4), `sdp-coordinator` (halt.generic at GPG halt), `sdp-reviewer` (milestone.task_verified, ships disabled). `milestone.phase_complete`/`milestone.all_complete`/`gate.blocked` are defined in `SDP-Tones.json` but **not yet wired** — no phase-gate execution exists in any skill (forward item). SKILLS CHECK (steps 0b ×2, Phase Gate 1b) now verifies `SDP-Tones.json` exists. |
| 0.9.4 | 0 | 2026-06-19 | Script authoring: new `SDP-Script-Authoring.md` companion document covering script vs. skill decision table, script anatomy (SYNOPSIS, workspace root resolution, parameter conventions, output and error/exit contracts, encoding), permission registration requirement with exact format and failure diagnosis, bootstrap integration guidance, creation checklist, and annotated worked examples for `sdp-tone.ps1` and `sdp-create-prompt.ps1`. `SDP-workspace-setup.md` updated: folder structure, "Verify script permissions" setup checklist item listing explicit `permissions.allow` entries for all current scripts, and "Script Authoring and Permission Gaps" bootstrap open items section. `SDP-Document-List.json` registered new doc. Root cause: Claude subagents do not inherit wildcard permissions — each script requires an explicit `permissions.allow` entry in `.claude/settings.local.json` or PowerShell tool calls are silently blocked in subagent context. |
| 0.9.3 | 0 | 2026-06-17 | Three changes: (1) `sdp-auto` sentinel `expected_status` mismatch now auto-invokes `/sdp-coordinator` then proceeds to Step 4 → Step 5 instead of halting; (2) WORKER adds commit-and-push step before session ends (short hash in Completed blockquote); REVIEWER adds git sweep sub-step before verification; COORDINATOR dispatch instructions note for CI acceptance criteria tasks; (3) SDP workflow folder renamed `docs/` → `sdp-docs/` to reserve `docs/` for project documentation — updated across all skills and bootstrap docs. |
| 0.9.2 | 0 | 2026-06-17 | Audible tone notifications: `sdp-tone` skill added (Level 1 + Level 2); `SDP-Config.json` restructured with `tones` section (`enabled`, `palette`, `assignments` array with per-skill start/end tone mapping and wildcard catch-all); `loop` key renamed to `loopInterval`; all Level 1 skill shims updated to invoke `/sdp-tone` at start (step 1) and end (step 7); `sdp-tone` added to SKILLS CHECK lists in all three validation points (two COORDINATOR checks and Phase Gate check). |
| 0.9.1 | 0 | 2026-06-17 | Deterministic sentinel-based loop dispatch: `sdp-docs/00_prompt.txt` now carries a `[sdp-prompt work_item="..." expected_status="..."]` sentinel line written by `sdp-create-prompt`; `sdp-state-loop` evaluates the sentinel against current workflow state to determine EXECUTE vs GENERATE — eliminates idle-loop condition on VERIFIED state. New `SDP-Config.json` user-editable config file at workspace root (`loop.interval_minutes`); `sdp-auto` reads it in Step 5 and validates the sentinel in Step 3. Workspace setup doc updated: `SDP-Config.json` template section and checklist item added. |
| 0.9.0 | 0 | 2026-06-16 | Three additions from user-note integration: `.speq` Contract mechanism (binding technical contract file, workspace setup checklist item, WORKER step 2a, REVIEWER step 6 alignment check, new `.speq` Contract section); Four-phase debugging escalation (DIAGNOSIS_BLOCKED flag, WORKER step 10, COORDINATOR dispatch step 4, flags array documentation, Invocation Reference update); No Placeholders standing instruction (WORKER step 0b). |
| 0.7.0 | 0 | 2026-06-09 | Session Start Hook section: agent instruction to check for and add the sdp-read-docs SessionStart hook in .claude/settings.json during workspace setup; sample hook code embedded; Setup Checklist item added. |
| 0.6.0 | 0 | 2026-06-09 | Version management rules: on first read, scan workspace root for newer-versioned bootstrap files and switch to the highest; when internal version and filename diverge, copy the file with the new version in the filename, update the File field in the copy, and treat the copy as authoritative. File field corrected to v0.5.0. |
| 0.5.0 | 0 | 2026-06-09 | GAP fixes: WORKER/REVIEWER role bullets corrected to phase-file model (GAP-1); WORKER and REVIEWER sessions both read dispatch file as step 2, subsequent steps renumbered (GAP-2 + REVIEWER parity); WORKER "confirm understanding" clarified to non-blocking (GAP-3); partially compliant state transition + PARTIAL_COMPLIANCE flag + escalation rule (GAP-4); pros-cons-gaps termination changed to 2-N with explicit gap resolution criteria (GAP-5); registry Depends On column + status notation corrected [-]=in-progress [x]=complete (GAP-6); COORDINATOR brainstorming boundary clarified to include section files (GAP-7); gate verdict blockquote format defined (GAP-8); session isolation model changed to subagent invocations (GAP-9); REVIEWER GPG alignment scope tied to GPG TOC cross-reference (GAP-10). Skill names prefixed sdp-. |
| 0.4.0 | 0 | 2026-06-09 | Superpowers plugin integration: per-role usage rules, SUPERPOWERS CHECK alongside GPG CHECK in all session types, TDD and four-phase debugging added as standing WORKER instructions, explicit prohibition of /execute-plan review checkpoints and auto-triggering, brainstorming capture rule for early phases, Setup Checklist install step |
| 0.3.0 | 0 | 2026-06-09 | Pre-Work Verification Protocol (scan/classify/act before every task); Re-evaluation Triggers (explicit criteria for Eval 2+ cycles); Eval/Verified semantic correction (Eval concludes compliant/partially/non-compliant; Verified written only after passing eval; non-compliant eval → REJECTED, no Verified written); ⚡ Deploy annotation pattern; Source Documentation Table (D-IDs for multi-source plans); Design Strengths template section; Scope Tag mechanism with Agent Scope Instruction box |
| 0.2.0 | 0 | 2026-06-09 | Added PATTERNS.md mechanism, Patterns Library section, gap resolution format with `[GENERIC PATTERN]` annotation, traceability IDs, cycle summary table, split TOC templates (Sections/Phases), audience column, emoji status values, orphan rule with parent-check, section file YAML frontmatter, combined sync+append-only notice, file organization diagram, quick search guide, why-separate-files rationale |
| 0.1.0 | 0 | 2026-06-09 | Initial draft — workflow philosophy, agent roles, state machine, document lifecycle, workspace setup templates, implementation loop, append-only discipline |

